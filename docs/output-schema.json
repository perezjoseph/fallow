{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Fallow JSON Output Schemas",
  "description": "Schemas for the JSON output of fallow commands. Bare `fallow --format json` produces a combined envelope with `check`, `dupes`, and `health` keys. Individual commands (`fallow dead-code --format json`, `fallow health --format json`, `fallow dupes --format json`, `fallow audit --format json`, `fallow explain <issue-type> --format json`, `fallow coverage setup --json`) produce their own top-level structure.",
  "oneOf": [
    {
      "$ref": "#/definitions/CombinedOutput"
    },
    {
      "$ref": "#/definitions/CheckOutput"
    },
    {
      "$ref": "#/definitions/CheckGroupedOutput"
    },
    {
      "$ref": "#/definitions/HealthOutput"
    },
    {
      "$ref": "#/definitions/DupesOutput"
    },
    {
      "$ref": "#/definitions/AuditOutput"
    },
    {
      "$ref": "#/definitions/ExplainOutput"
    },
    {
      "$ref": "#/definitions/CoverageSetupOutput"
    },
    {
      "$ref": "#/definitions/CodeClimateOutput"
    },
    {
      "$ref": "#/definitions/ReviewEnvelopeOutput"
    },
    {
      "$ref": "#/definitions/ReviewReconcileOutput"
    }
  ],
  "definitions": {
    "CombinedOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Schema version for this output format.",
          "allOf": [
            {
              "$ref": "#/definitions/SchemaVersion"
            }
          ]
        },
        "version": {
          "description": "Fallow tool version that produced this output.",
          "allOf": [
            {
              "$ref": "#/definitions/ToolVersion"
            }
          ]
        },
        "elapsed_ms": {
          "description": "Analysis duration in milliseconds.",
          "allOf": [
            {
              "$ref": "#/definitions/ElapsedMs"
            }
          ]
        },
        "check": {
          "anyOf": [
            {
              "$ref": "#/definitions/CheckOutput"
            },
            {
              "type": "null"
            }
          ],
          "description": "Dead-code analysis sub-envelope. Absent when `--skip check`."
        },
        "dupes": {
          "anyOf": [
            {
              "$ref": "#/definitions/DuplicationReport"
            },
            {
              "type": "null"
            }
          ],
          "description": "Duplication analysis body (bare `DuplicationReport`, not the full\n`DupesOutput` envelope). Absent when `--skip dupes`."
        },
        "health": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthReport"
            },
            {
              "type": "null"
            }
          ],
          "description": "Complexity analysis body (bare `HealthReport`, not the full\n`HealthOutput` envelope). Absent when `--skip health`."
        }
      },
      "required": [
        "schema_version",
        "version",
        "elapsed_ms"
      ],
      "description": "Envelope emitted by bare `fallow --format json` (the combined\ninvocation). Wraps the per-analysis sub-results inside a single envelope\nwith the standard `schema_version` / `version` / `elapsed_ms` header.\n\nEach sub-result is `Option<...>` so `--only` / `--skip` can suppress a\npass without leaving an empty key on the wire. The `check` sub-result is\nthe full [`CheckOutput`] envelope (including its own `schema_version` /\n`version` / `elapsed_ms`), but `dupes` and `health` are the bare body\ntypes: the runtime emit calls `serde_json::to_value(&report)` on\n`DuplicationReport` / `HealthReport` directly rather than wrapping them\nin their per-command envelope. The committed schema points `dupes` at\n`#/definitions/DuplicationReport` and `health` at\n`#/definitions/HealthReport` so the documented shape matches the\nwire; the `committed_property_refs_match_derived_property_refs`\ndrift test enforces the alignment."
    },
    "AuditOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Schema version for this output format.",
          "allOf": [
            {
              "$ref": "#/definitions/SchemaVersion"
            }
          ]
        },
        "version": {
          "description": "Fallow tool version that produced this output.",
          "allOf": [
            {
              "$ref": "#/definitions/ToolVersion"
            }
          ]
        },
        "command": {
          "description": "Singleton command discriminator (always `\"audit\"`).",
          "allOf": [
            {
              "$ref": "#/definitions/AuditCommand"
            }
          ]
        },
        "verdict": {
          "description": "Overall verdict: `pass` (no issues), `warn` (warn-severity only,\nexit 0), or `fail` (error-severity issues, exit 1).",
          "allOf": [
            {
              "$ref": "#/definitions/AuditVerdict"
            }
          ]
        },
        "changed_files_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Number of files changed between base ref and HEAD."
        },
        "base_ref": {
          "type": "string",
          "description": "Git ref used as comparison base (explicit or auto-detected)."
        },
        "head_sha": {
          "type": [
            "string",
            "null"
          ],
          "description": "Short SHA of HEAD. Omitted when git is unavailable."
        },
        "elapsed_ms": {
          "description": "Analysis duration in milliseconds.",
          "allOf": [
            {
              "$ref": "#/definitions/ElapsedMs"
            }
          ]
        },
        "base_snapshot_skipped": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Only emitted when `--performance` is set. `true` means audit reused\nthe current run's keys as the base snapshot (the docs-only-diff\nfast path); `false` means the regular base worktree analysis ran."
        },
        "summary": {
          "description": "Per-category summary counts.",
          "allOf": [
            {
              "$ref": "#/definitions/AuditSummary"
            }
          ]
        },
        "attribution": {
          "description": "Counts split by whether each finding was introduced by the current\nchangeset or already existed at the base ref.",
          "allOf": [
            {
              "$ref": "#/definitions/AuditAttribution"
            }
          ]
        },
        "dead_code": {
          "anyOf": [
            {
              "$ref": "#/definitions/CheckOutput"
            },
            {
              "type": "null"
            }
          ],
          "description": "Full dead-code results. Absent when no changed files."
        },
        "duplication": {
          "anyOf": [
            {
              "$ref": "#/definitions/DuplicationReport"
            },
            {
              "type": "null"
            }
          ],
          "description": "Full duplication results (bare body, not the full `DupesOutput`\nenvelope). Absent when no changed files."
        },
        "complexity": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthReport"
            },
            {
              "type": "null"
            }
          ],
          "description": "Full complexity results (bare body, not the full `HealthOutput`\nenvelope). Absent when no changed files."
        }
      },
      "required": [
        "schema_version",
        "version",
        "command",
        "verdict",
        "changed_files_count",
        "base_ref",
        "elapsed_ms",
        "summary",
        "attribution"
      ],
      "description": "Envelope emitted by `fallow audit --format json`. Combines dead code,\ncomplexity, and duplication scoped to changed files with a verdict\n(`pass` / `warn` / `fail`), a per-category summary, optional\nnew-vs-inherited attribution, and full sub-results.\n\nLike [`CombinedOutput`], `audit`'s `duplication` and `complexity`\nsub-keys hold bare body types (`DuplicationReport` / `HealthReport`)\nrather than the per-command envelope shapes; `dead_code` is the full\n[`CheckOutput`] envelope. The committed schema points `duplication`\nat `#/definitions/DuplicationReport` and `complexity` at\n`#/definitions/HealthReport` so the documented shape matches the\nwire; the `committed_property_refs_match_derived_property_refs`\ndrift test enforces the alignment."
    },
    "AuditIntroduced": {
      "type": "boolean",
      "description": "Audit-mode marker emitted on each finding when `fallow audit --format json`\nruns with a base ref. `true` means the finding's structural key was not\npresent at the base ref (introduced by the current changeset); `false`\nmeans it was inherited.\n\nOutside of audit sub-results the field is omitted, so call sites typically\nhold `Option<AuditIntroduced>`. Renders to the JSON wire as a bare boolean."
    },
    "ExplainOutput": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "description": "Canonical rule id, for example `fallow/unused-export`."
        },
        "name": {
          "type": "string",
          "description": "Human-readable rule name."
        },
        "summary": {
          "type": "string",
          "description": "Short one-line explanation of the issue."
        },
        "rationale": {
          "type": "string",
          "description": "Why the issue matters and what fallow checks."
        },
        "example": {
          "type": "string",
          "description": "Concrete example of the finding."
        },
        "how_to_fix": {
          "type": "string",
          "description": "Recommended fix or suppression guidance."
        },
        "docs": {
          "type": "string",
          "description": "Docs URL for the rule."
        }
      },
      "required": [
        "id",
        "name",
        "summary",
        "rationale",
        "example",
        "how_to_fix",
        "docs"
      ],
      "description": "Envelope emitted by `fallow explain <issue-type> --format json`.\n\nStandalone rule explanation. This command does not run project analysis\nand intentionally returns a compact object without `schema_version` /\n`version` metadata; consumers that need those should call any other\nfallow JSON-producing command."
    },
    "CoverageSetupOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Standalone coverage setup envelope version (always `\"1\"`).",
          "allOf": [
            {
              "$ref": "#/definitions/CoverageSetupSchemaVersion"
            }
          ]
        },
        "framework_detected": {
          "description": "Primary detected runtime framework. For workspaces this mirrors the\nfirst emitted runtime member; `unknown` means no runtime member was\ndetected.",
          "allOf": [
            {
              "$ref": "#/definitions/CoverageSetupFramework"
            }
          ]
        },
        "package_manager": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageSetupPackageManager"
            },
            {
              "type": "null"
            }
          ],
          "description": "Detected JavaScript package manager. `null` when none could be\nresolved."
        },
        "runtime_targets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupRuntimeTarget"
          },
          "description": "Union of runtime targets across emitted members."
        },
        "members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupMember"
          },
          "description": "Per-runtime-workspace setup recipes. Pure aggregator roots and\nbuild-only library packages are omitted."
        },
        "config_written": {
          "description": "Always `null` today. Reserved for a future \"config has been written\nto disk\" indicator."
        },
        "commands": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Shell commands the agent should run from the workspace root."
        },
        "files_to_edit": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupFileToEdit"
          },
          "description": "Compatibility copy of the primary member's files, with workspace\nprefixes when the primary member is not the root."
        },
        "snippets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupSnippet"
          },
          "description": "Compatibility copy of the primary member's snippets, with workspace\nprefixes when the primary member is not the root."
        },
        "dockerfile_snippet": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional Dockerfile RUN/COPY snippet to enable the beacon in\ncontainerised deployments."
        },
        "next_steps": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Ordered next-step instructions for the agent / human operator."
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Non-fatal warnings raised during setup detection."
        },
        "_meta": {
          "description": "`_meta` block emitted only when `--explain` is passed."
        }
      },
      "required": [
        "schema_version",
        "framework_detected",
        "runtime_targets",
        "members",
        "commands",
        "files_to_edit",
        "snippets",
        "next_steps",
        "warnings"
      ],
      "description": "Envelope emitted by `fallow coverage setup --json`. Deterministic\nagent-readable runtime coverage setup instructions. In workspaces,\n`members` carries one entry per detected runtime package; `runtime_targets`\nis the union of all member targets.\n\nThe runtime path in `crates/cli/src/coverage/mod.rs::build_setup_json`\nstill constructs the wire shape via `serde_json::json!` macros (one per\nmember, snippet, and file-to-edit). The typed struct here serves as the\nschema source of truth via the drift gate; a follow-up can swap the\nruntime over without changing the wire."
    },
    "CoverageSetupMember": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Workspace package name (or root marker for single-package projects)."
        },
        "path": {
          "type": "string",
          "description": "Workspace path relative to the analysed root, or `.` for the root\nmember."
        },
        "framework_detected": {
          "description": "Framework detected for this member.",
          "allOf": [
            {
              "$ref": "#/definitions/CoverageSetupFramework"
            }
          ]
        },
        "package_manager": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageSetupPackageManager"
            },
            {
              "type": "null"
            }
          ],
          "description": "Package manager detected for this member."
        },
        "runtime_targets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupRuntimeTarget"
          },
          "description": "Runtime targets supported by this member's framework."
        },
        "files_to_edit": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupFileToEdit"
          },
          "description": "Files the agent should edit to wire in the beacon."
        },
        "snippets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CoverageSetupSnippet"
          },
          "description": "Code snippets the agent should paste into the edited files."
        },
        "dockerfile_snippet": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional Dockerfile snippet specific to this member."
        },
        "warnings": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Member-scoped warnings."
        }
      },
      "required": [
        "name",
        "path",
        "framework_detected",
        "runtime_targets",
        "files_to_edit",
        "snippets",
        "warnings"
      ],
      "description": "Per-workspace setup recipe inside [`CoverageSetupOutput::members`]."
    },
    "CoverageSetupFileToEdit": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Workspace-relative path to the file to edit."
        },
        "reason": {
          "type": "string",
          "description": "Why the file needs editing (e.g. `\"Mount the beacon middleware\"`)."
        }
      },
      "required": [
        "path",
        "reason"
      ],
      "description": "Single file to edit inside [`CoverageSetupMember::files_to_edit`] or\n[`CoverageSetupOutput::files_to_edit`]."
    },
    "CoverageSetupSnippet": {
      "type": "object",
      "properties": {
        "label": {
          "type": "string",
          "description": "Short label identifying the snippet (used by the human renderer)."
        },
        "path": {
          "type": "string",
          "description": "Workspace-relative path the snippet should be pasted into."
        },
        "content": {
          "type": "string",
          "description": "Snippet content (literal source text)."
        }
      },
      "required": [
        "label",
        "path",
        "content"
      ],
      "description": "Single code snippet inside [`CoverageSetupMember::snippets`] or\n[`CoverageSetupOutput::snippets`]."
    },
    "CheckOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Schema version for this output format.",
          "allOf": [
            {
              "$ref": "#/definitions/SchemaVersion"
            }
          ]
        },
        "version": {
          "description": "Fallow tool version that produced this output.",
          "allOf": [
            {
              "$ref": "#/definitions/ToolVersion"
            }
          ]
        },
        "elapsed_ms": {
          "description": "Analysis duration in milliseconds.",
          "allOf": [
            {
              "$ref": "#/definitions/ElapsedMs"
            }
          ]
        },
        "total_issues": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of issues found across all categories."
        },
        "entry_points": {
          "anyOf": [
            {
              "$ref": "#/definitions/EntryPoints"
            },
            {
              "type": "null"
            }
          ],
          "description": "Entry-point detection summary. Present when the analysis populated\nthe metadata block; absent in synthesised fixtures."
        },
        "summary": {
          "description": "Per-category issue counts. Always present in real runs.",
          "allOf": [
            {
              "$ref": "#/definitions/CheckSummary"
            }
          ]
        },
        "unused_files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedFile"
          },
          "description": "Files not reachable from any entry point."
        },
        "unused_exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedExport"
          },
          "description": "Exports never imported by other modules."
        },
        "unused_types": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedExport"
          },
          "description": "Type exports never imported by other modules."
        },
        "private_type_leaks": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PrivateTypeLeak"
          },
          "description": "Exported symbols whose public signature references same-file private types."
        },
        "unused_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Dependencies listed in package.json but never imported."
        },
        "unused_dev_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Dev dependencies listed in package.json but never imported."
        },
        "unused_optional_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Optional dependencies listed in package.json but never imported."
        },
        "unused_enum_members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedMember"
          },
          "description": "Enum members never accessed."
        },
        "unused_class_members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedMember"
          },
          "description": "Class members never accessed."
        },
        "unresolved_imports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnresolvedImport"
          },
          "description": "Import specifiers that could not be resolved."
        },
        "unlisted_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnlistedDependency"
          },
          "description": "Dependencies used in code but not listed in package.json."
        },
        "duplicate_exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DuplicateExport"
          },
          "description": "Exports with the same name across multiple modules."
        },
        "type_only_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TypeOnlyDependency"
          },
          "description": "Production dependencies only used via type-only imports (could be devDependencies).\nOnly populated in production mode."
        },
        "test_only_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TestOnlyDependency"
          },
          "description": "Production dependencies only imported by test files (could be devDependencies).",
          "default": []
        },
        "circular_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CircularDependency"
          },
          "description": "Circular dependency chains detected in the module graph."
        },
        "boundary_violations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/BoundaryViolation"
          },
          "description": "Imports that cross architecture boundary rules.",
          "default": []
        },
        "stale_suppressions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/StaleSuppression"
          },
          "description": "Suppression comments or JSDoc tags that no longer match any issue.",
          "default": []
        },
        "unused_catalog_entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedCatalogEntry"
          },
          "description": "Entries in pnpm-workspace.yaml catalogs that no workspace package references.",
          "default": []
        },
        "empty_catalog_groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EmptyCatalogGroup"
          },
          "description": "Empty named groups under pnpm-workspace.yaml's catalogs: section.",
          "default": []
        },
        "unresolved_catalog_references": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnresolvedCatalogReference"
          },
          "description": "Workspace package.json references to pnpm catalogs that don't declare the package.",
          "default": []
        },
        "unused_dependency_overrides": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependencyOverride"
          },
          "description": "Entries in pnpm `overrides:` / `pnpm.overrides` whose target package is not\ndeclared by any workspace package and not resolved in pnpm-lock.yaml.",
          "default": []
        },
        "misconfigured_dependency_overrides": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MisconfiguredDependencyOverride"
          },
          "description": "Entries in pnpm `overrides:` / `pnpm.overrides` whose key or value cannot be parsed.",
          "default": []
        },
        "baseline_deltas": {
          "anyOf": [
            {
              "$ref": "#/definitions/BaselineDeltas"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-category delta comparison against a saved baseline. Only present\nwhen `--baseline` is used (today only via the combined invocation)."
        },
        "baseline": {
          "anyOf": [
            {
              "$ref": "#/definitions/BaselineMatch"
            },
            {
              "type": "null"
            }
          ],
          "description": "Baseline match statistics. Only present when `--baseline` is used."
        },
        "regression": {
          "anyOf": [
            {
              "$ref": "#/definitions/RegressionResult"
            },
            {
              "type": "null"
            }
          ],
          "description": "Regression check result. Only present when `--fail-on-regression` is\nused."
        },
        "_meta": {
          "anyOf": [
            {
              "$ref": "#/definitions/Meta"
            },
            {
              "type": "null"
            }
          ],
          "description": "`_meta` block with metric / rule definitions, emitted when `--explain`\nis passed (always present in MCP responses)."
        }
      },
      "required": [
        "schema_version",
        "version",
        "elapsed_ms",
        "total_issues",
        "summary",
        "unused_files",
        "unused_exports",
        "unused_types",
        "private_type_leaks",
        "unused_dependencies",
        "unused_dev_dependencies",
        "unused_optional_dependencies",
        "unused_enum_members",
        "unused_class_members",
        "unresolved_imports",
        "unlisted_dependencies",
        "duplicate_exports",
        "type_only_dependencies",
        "circular_dependencies"
      ],
      "description": "Envelope emitted by `fallow dead-code --format json` (plus the `check`\nblock inside the combined and audit envelopes).\n\nThe body is the full `AnalysisResults` flattened into the envelope so\nevery issue array (`unused_files`, `unused_exports`, ...) lives at the\ntop level, matching the existing wire shape. `entry_points` lifts the\notherwise `#[serde(skip)]`'d `AnalysisResults::entry_point_summary` back\ninto the JSON output. `summary` carries the per-category counts the\nJSON layer always emits."
    },
    "CheckGroupedOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Schema version for this output format.",
          "allOf": [
            {
              "$ref": "#/definitions/SchemaVersion"
            }
          ]
        },
        "version": {
          "description": "Fallow tool version that produced this output.",
          "allOf": [
            {
              "$ref": "#/definitions/ToolVersion"
            }
          ]
        },
        "elapsed_ms": {
          "description": "Analysis duration in milliseconds.",
          "allOf": [
            {
              "$ref": "#/definitions/ElapsedMs"
            }
          ]
        },
        "grouped_by": {
          "description": "The grouping strategy used.",
          "allOf": [
            {
              "$ref": "#/definitions/GroupByMode"
            }
          ]
        },
        "total_issues": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of issues across all groups."
        },
        "groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CheckGroupedEntry"
          },
          "description": "One entry per group; each contains the same issue arrays as\n`CheckOutput` plus the group key and per-group total."
        },
        "_meta": {
          "anyOf": [
            {
              "$ref": "#/definitions/Meta"
            },
            {
              "type": "null"
            }
          ],
          "description": "`_meta` block with metric / rule definitions, emitted when `--explain`\nis passed."
        }
      },
      "required": [
        "schema_version",
        "version",
        "elapsed_ms",
        "grouped_by",
        "total_issues",
        "groups"
      ],
      "description": "Envelope emitted by `fallow dead-code --group-by ... --format json`.\n\nIssues are partitioned into resolver buckets (CODEOWNERS team, directory\nprefix, workspace package, or GitLab CODEOWNERS section) instead of flat\narrays. Each bucket carries the same issue-array shape as the ungrouped\n`CheckOutput` body, plus per-group `key` / `owners` / `total_issues`."
    },
    "CheckGroupedEntry": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "Group identifier produced by the resolver. For `package` grouping:\nworkspace package name. For `owner` grouping: the CODEOWNERS team.\nFor `directory` grouping: the top-level directory prefix. For\n`section` grouping: the GitLab CODEOWNERS section name (or\n`(no section)` / `(unowned)` for unmatched files)."
        },
        "owners": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "Section default owners (GitLab CODEOWNERS `[Section] @owner1\n@owner2`). Emitted only when `grouped_by` is `section`. Empty for\nthe `(no section)` and `(unowned)` buckets."
        },
        "total_issues": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of issues in this group."
        },
        "unused_files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedFile"
          },
          "description": "Files not reachable from any entry point."
        },
        "unused_exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedExport"
          },
          "description": "Exports never imported by other modules."
        },
        "unused_types": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedExport"
          },
          "description": "Type exports never imported by other modules."
        },
        "private_type_leaks": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PrivateTypeLeak"
          },
          "description": "Exported symbols whose public signature references same-file private types."
        },
        "unused_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Dependencies listed in package.json but never imported."
        },
        "unused_dev_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Dev dependencies listed in package.json but never imported."
        },
        "unused_optional_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Optional dependencies listed in package.json but never imported."
        },
        "unused_enum_members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedMember"
          },
          "description": "Enum members never accessed."
        },
        "unused_class_members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedMember"
          },
          "description": "Class members never accessed."
        },
        "unresolved_imports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnresolvedImport"
          },
          "description": "Import specifiers that could not be resolved."
        },
        "unlisted_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnlistedDependency"
          },
          "description": "Dependencies used in code but not listed in package.json."
        },
        "duplicate_exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DuplicateExport"
          },
          "description": "Exports with the same name across multiple modules."
        },
        "type_only_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TypeOnlyDependency"
          },
          "description": "Production dependencies only used via type-only imports (could be devDependencies).\nOnly populated in production mode."
        },
        "test_only_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TestOnlyDependency"
          },
          "description": "Production dependencies only imported by test files (could be devDependencies).",
          "default": []
        },
        "circular_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CircularDependency"
          },
          "description": "Circular dependency chains detected in the module graph."
        },
        "boundary_violations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/BoundaryViolation"
          },
          "description": "Imports that cross architecture boundary rules.",
          "default": []
        },
        "stale_suppressions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/StaleSuppression"
          },
          "description": "Suppression comments or JSDoc tags that no longer match any issue.",
          "default": []
        },
        "unused_catalog_entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedCatalogEntry"
          },
          "description": "Entries in pnpm-workspace.yaml catalogs that no workspace package references.",
          "default": []
        },
        "empty_catalog_groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EmptyCatalogGroup"
          },
          "description": "Empty named groups under pnpm-workspace.yaml's catalogs: section.",
          "default": []
        },
        "unresolved_catalog_references": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnresolvedCatalogReference"
          },
          "description": "Workspace package.json references to pnpm catalogs that don't declare the package.",
          "default": []
        },
        "unused_dependency_overrides": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependencyOverride"
          },
          "description": "Entries in pnpm `overrides:` / `pnpm.overrides` whose target package is not\ndeclared by any workspace package and not resolved in pnpm-lock.yaml.",
          "default": []
        },
        "misconfigured_dependency_overrides": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MisconfiguredDependencyOverride"
          },
          "description": "Entries in pnpm `overrides:` / `pnpm.overrides` whose key or value cannot be parsed.",
          "default": []
        }
      },
      "required": [
        "key",
        "total_issues",
        "unused_files",
        "unused_exports",
        "unused_types",
        "private_type_leaks",
        "unused_dependencies",
        "unused_dev_dependencies",
        "unused_optional_dependencies",
        "unused_enum_members",
        "unused_class_members",
        "unresolved_imports",
        "unlisted_dependencies",
        "duplicate_exports",
        "type_only_dependencies",
        "circular_dependencies"
      ],
      "description": "Single resolver bucket inside `CheckGroupedOutput`. Carries the group's\nidentifier, optional section owners, and a per-group flattened\n`AnalysisResults`."
    },
    "HealthOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Schema version for this output format.",
          "allOf": [
            {
              "$ref": "#/definitions/SchemaVersion"
            }
          ]
        },
        "version": {
          "description": "Fallow tool version that produced this output.",
          "allOf": [
            {
              "$ref": "#/definitions/ToolVersion"
            }
          ]
        },
        "elapsed_ms": {
          "description": "Analysis duration in milliseconds.",
          "allOf": [
            {
              "$ref": "#/definitions/ElapsedMs"
            }
          ]
        },
        "findings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HealthFinding"
          },
          "description": "Functions exceeding thresholds."
        },
        "summary": {
          "description": "Summary statistics.",
          "allOf": [
            {
              "$ref": "#/definitions/HealthSummary"
            }
          ]
        },
        "vital_signs": {
          "anyOf": [
            {
              "$ref": "#/definitions/VitalSigns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Project-wide vital signs (always computed from available data)."
        },
        "health_score": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthScore"
            },
            {
              "type": "null"
            }
          ],
          "description": "Project-wide health score (only populated with `--score`)."
        },
        "file_scores": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/FileHealthScore"
          },
          "description": "Per-file health scores (only populated with `--file-scores` or `--hotspots`)."
        },
        "coverage_gaps": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageGaps"
            },
            {
              "type": "null"
            }
          ],
          "description": "Static coverage gaps.\n\nPopulated when coverage gaps are explicitly requested, or when the\ntop-level `health` command allows config severity to surface them in the\ndefault report."
        },
        "hotspots": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HotspotEntry"
          },
          "description": "Hotspot entries (only populated with `--hotspots`)."
        },
        "hotspot_summary": {
          "anyOf": [
            {
              "$ref": "#/definitions/HotspotSummary"
            },
            {
              "type": "null"
            }
          ],
          "description": "Hotspot analysis summary (only set with `--hotspots`)."
        },
        "runtime_coverage": {
          "anyOf": [
            {
              "$ref": "#/definitions/RuntimeCoverageReport"
            },
            {
              "type": "null"
            }
          ],
          "description": "Runtime coverage findings from the paid sidecar (only populated with\n`--runtime-coverage`)."
        },
        "large_functions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LargeFunctionEntry"
          },
          "description": "Functions exceeding 60 LOC (only populated when unit size very-high-risk >= 3%)."
        },
        "targets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RefactoringTarget"
          },
          "description": "Ranked refactoring recommendations (only populated with `--targets`)."
        },
        "target_thresholds": {
          "anyOf": [
            {
              "$ref": "#/definitions/TargetThresholds"
            },
            {
              "type": "null"
            }
          ],
          "description": "Adaptive thresholds used for target scoring (only set with `--targets`)."
        },
        "health_trend": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthTrend"
            },
            {
              "type": "null"
            }
          ],
          "description": "Health trend comparison against a previous snapshot (only set with `--trend`)."
        },
        "grouped_by": {
          "anyOf": [
            {
              "$ref": "#/definitions/GroupByMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "Resolver mode used when `--group-by` is active. Absent on ungrouped\noutput."
        },
        "groups": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "$ref": "#/definitions/HealthGroup"
          },
          "description": "Per-group health output, present only when `--group-by` is active.\nEach group recomputes its own `vital_signs` and `health_score` from\nthe files in that group, mirroring how `--workspace` scopes a single\nsubset."
        },
        "_meta": {
          "anyOf": [
            {
              "$ref": "#/definitions/Meta"
            },
            {
              "type": "null"
            }
          ],
          "description": "`_meta` block with metric / rule definitions, emitted when `--explain`\nis passed (always present in MCP responses)."
        }
      },
      "required": [
        "schema_version",
        "version",
        "elapsed_ms",
        "findings",
        "summary"
      ],
      "description": "Envelope emitted by `fallow health --format json` (plus the `health` block\ninside the combined and audit envelopes).\n\nThe body is `HealthReport` flattened into the envelope so every report\nfield (`findings`, `summary`, `vital_signs`, `hotspots`, ...) lives at the\ntop level. Grouped runs populate `grouped_by` + `groups` with per-bucket\nrecomputed metrics. The `actions_meta` breadcrumb is NOT modeled here:\n`inject_health_actions` adds it as a post-pass on the `serde_json::Value`\ntree, and the drift gate tolerates the gap via its `AUGMENTATION_KEYS`\nlist because the typed wrapper would force every caller to plumb the\nsuppression context through, which buys nothing today."
    },
    "HealthGroup": {
      "type": "object",
      "properties": {
        "key": {
          "type": "string",
          "description": "Group label."
        },
        "owners": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "Section default owners (`--group-by section` only)."
        },
        "files_analyzed": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Files participating in this group after workspace and ignore filters."
        },
        "functions_above_threshold": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of findings in this group, mirroring the project-level\n`summary.functions_above_threshold` semantics post-baseline /\npost-`--top` truncation. When `--top` was supplied this reflects the\nrendered finding count, not the un-truncated total."
        },
        "vital_signs": {
          "anyOf": [
            {
              "$ref": "#/definitions/VitalSigns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-group vital signs (None when `--score-only` suppressed them)."
        },
        "health_score": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthScore"
            },
            {
              "type": "null"
            }
          ],
          "description": "Per-group health score (None when `--score` was not requested)."
        },
        "findings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HealthFinding"
          },
          "description": "Findings restricted to files in this group."
        },
        "file_scores": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/FileHealthScore"
          },
          "description": "File scores restricted to files in this group."
        },
        "hotspots": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HotspotEntry"
          },
          "description": "Hotspots restricted to files in this group."
        },
        "large_functions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LargeFunctionEntry"
          },
          "description": "Large functions in files belonging to this group."
        },
        "targets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RefactoringTarget"
          },
          "description": "Refactoring targets in files belonging to this group."
        }
      },
      "required": [
        "key",
        "files_analyzed",
        "functions_above_threshold"
      ],
      "description": "A health report scoped to a single group.\n\n`key` is the group label produced by the resolver (workspace package name,\nCODEOWNERS owner, directory, or section). `owners` is populated only for\n`--group-by section` (mirrors dead-code grouped output).\n\nPer-group `vital_signs` and `health_score` are recomputed from the\nfiles in the group, so they answer \"what is the health of workspace X\" in\na single invocation. `files_analyzed` and `functions_above_threshold`\nsummarise the subset for parity with the project-level\n[`crate::health_types::HealthSummary`]."
    },
    "RuntimeCoverageReport": {
      "type": "object",
      "properties": {
        "verdict": {
          "$ref": "#/definitions/RuntimeCoverageReportVerdict"
        },
        "signals": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageSignal"
          },
          "description": "All signals captured by post-processing. Independent of `verdict`,\nwhich is the single most actionable signal under the current\ncontext. Empty when the report is `Clean` and not under license\ngrace. Order is stable severity-descending."
        },
        "summary": {
          "$ref": "#/definitions/RuntimeCoverageSummary"
        },
        "findings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageFinding"
          }
        },
        "hot_paths": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageHotPath"
          }
        },
        "blast_radius": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageBlastRadiusEntry"
          }
        },
        "importance": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageImportanceEntry"
          }
        },
        "watermark": {
          "anyOf": [
            {
              "$ref": "#/definitions/RuntimeCoverageWatermark"
            },
            {
              "type": "null"
            }
          ]
        },
        "warnings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageMessage"
          }
        },
        "schema_version": {
          "type": "string",
          "enum": [
            "1"
          ],
          "description": "Runtime coverage JSON contract version. Independent of the top-level fallow JSON schema_version."
        }
      },
      "required": [
        "verdict",
        "summary",
        "blast_radius",
        "importance",
        "schema_version"
      ]
    },
    "CoverageAnalyzeOutput": {
      "title": "fallow coverage analyze --format json",
      "description": "Focused runtime coverage analysis output. Local mode reads --runtime-coverage <path>. Cloud mode requires explicit --cloud / --runtime-coverage-cloud or FALLOW_RUNTIME_COVERAGE_SOURCE=cloud; FALLOW_API_KEY alone does not select cloud mode.",
      "type": "object",
      "required": [
        "schema_version",
        "version",
        "elapsed_ms",
        "runtime_coverage"
      ],
      "properties": {
        "schema_version": {
          "type": "string",
          "enum": [
            "1"
          ],
          "description": "Standalone coverage analyze envelope version."
        },
        "version": {
          "type": "string",
          "description": "fallow CLI version."
        },
        "elapsed_ms": {
          "type": "integer",
          "minimum": 0
        },
        "runtime_coverage": {
          "$ref": "#/definitions/RuntimeCoverageReport",
          "description": "The same runtime coverage block emitted by health JSON."
        },
        "_meta": {
          "$ref": "#/definitions/Meta"
        }
      }
    },
    "RuntimeCoverageSummary": {
      "type": "object",
      "properties": {
        "data_source": {
          "$ref": "#/definitions/RuntimeCoverageDataSource"
        },
        "last_received_at": {
          "type": [
            "string",
            "null"
          ]
        },
        "functions_tracked": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "functions_hit": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "functions_unhit": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "functions_untracked": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "coverage_percent": {
          "type": "number",
          "format": "double"
        },
        "trace_count": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "period_days": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "deployments_seen": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "capture_quality": {
          "anyOf": [
            {
              "$ref": "#/definitions/RuntimeCoverageCaptureQuality"
            },
            {
              "type": "null"
            }
          ],
          "description": "Capture-quality telemetry. `None` for protocol-0.2 sidecars; protocol-0.3+\nsidecars always populate it. Fuels the human-output short-window warning\nand the quantified trial CTA, and is passed through to JSON consumers so\nagent pipelines can surface the same signal."
        }
      },
      "required": [
        "data_source",
        "functions_tracked",
        "functions_hit",
        "functions_unhit",
        "functions_untracked",
        "coverage_percent",
        "trace_count",
        "period_days",
        "deployments_seen"
      ],
      "description": "Summary block mirroring `fallow_cov_protocol::Summary` (0.3 shape)."
    },
    "RuntimeCoverageCaptureQuality": {
      "type": "object",
      "properties": {
        "window_seconds": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "instances_observed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "lazy_parse_warning": {
          "type": "boolean"
        },
        "untracked_ratio_percent": {
          "type": "number",
          "format": "double"
        }
      },
      "required": [
        "window_seconds",
        "instances_observed",
        "lazy_parse_warning",
        "untracked_ratio_percent"
      ],
      "description": "Capture-quality telemetry (mirrors `fallow_cov_protocol::CaptureQuality`)."
    },
    "RuntimeCoverageReportVerdict": {
      "type": "string",
      "enum": [
        "clean",
        "hot-path-touched",
        "cold-code-detected",
        "license-expired-grace",
        "unknown"
      ],
      "description": "Top-level verdict for the whole runtime-coverage report. Mirrors\n`fallow_cov_protocol::ReportVerdict`. The verdict is the SINGLE most\nactionable finding; for the full set of findings see\n[`RuntimeCoverageReport::signals`]. The verdict promotes `hot-path-touched`\nabove `cold-code-detected` in PR-review context (when the CLI was\ngiven a change-scope: `--diff-file` or `--changed-since`) because the\ntouched-hot-path is event-tied to the current diff and reviewers need\nit to be the top-line signal. In standalone analysis (no change\nscope), `cold-code-detected` remains primary."
    },
    "RuntimeCoverageVerdict": {
      "type": "string",
      "enum": [
        "safe_to_delete",
        "review_required",
        "coverage_unavailable",
        "low_traffic",
        "active",
        "unknown"
      ],
      "description": "Per-finding verdict. Replaces the 0.1 `state` field."
    },
    "RuntimeCoverageSignal": {
      "type": "string",
      "enum": [
        "license-expired-grace",
        "cold-code-detected",
        "hot-path-touched"
      ],
      "description": "Discrete signal captured during runtime-coverage post-processing.\n`verdict` collapses to one summary value; `signals` enumerates ALL\nfindings the report carries so JSON consumers, CI dashboards, and\nagents can reason about them independently of the headline. Order is\nstable: severity-descending so the first entry mirrors a sensible\nnon-PR-context verdict."
    },
    "RuntimeCoverageConfidence": {
      "type": "string",
      "enum": [
        "very_high",
        "high",
        "medium",
        "low",
        "none",
        "unknown"
      ]
    },
    "RuntimeCoverageEvidence": {
      "type": "object",
      "properties": {
        "static_status": {
          "type": "string"
        },
        "test_coverage": {
          "type": "string"
        },
        "v8_tracking": {
          "type": "string"
        },
        "untracked_reason": {
          "type": [
            "string",
            "null"
          ]
        },
        "observation_days": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "deployments_observed": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        }
      },
      "required": [
        "static_status",
        "test_coverage",
        "v8_tracking",
        "observation_days",
        "deployments_observed"
      ],
      "description": "Supporting evidence for a finding (mirrors `fallow_cov_protocol::Evidence`)."
    },
    "RuntimeCoverageWatermark": {
      "type": "string",
      "enum": [
        "trial-expired",
        "license-expired-grace",
        "unknown"
      ]
    },
    "RuntimeCoverageAction": {
      "type": "object",
      "properties": {
        "type": {
          "type": "string",
          "description": "Stable action identifier. Serialized as `type` in JSON to match the\n`actions[].type` contract shared with every other `fallow health` finding."
        },
        "description": {
          "type": "string"
        },
        "auto_fixable": {
          "type": "boolean"
        }
      },
      "required": [
        "type",
        "description",
        "auto_fixable"
      ]
    },
    "RuntimeCoverageMessage": {
      "type": "object",
      "properties": {
        "code": {
          "type": "string"
        },
        "message": {
          "type": "string"
        }
      },
      "required": [
        "code",
        "message"
      ]
    },
    "RuntimeCoverageFinding": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable content-hash ID of the form `fallow:prod:<hash>`."
        },
        "path": {
          "type": "string"
        },
        "function": {
          "type": "string"
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "verdict": {
          "$ref": "#/definitions/RuntimeCoverageVerdict"
        },
        "invocations": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint64",
          "minimum": 0,
          "description": "Raw V8 invocation count. `None` when the function was untracked\n(lazy-parsed, worker thread, or dynamic code)."
        },
        "confidence": {
          "$ref": "#/definitions/RuntimeCoverageConfidence"
        },
        "evidence": {
          "$ref": "#/definitions/RuntimeCoverageEvidence"
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageAction"
          }
        }
      },
      "required": [
        "id",
        "path",
        "function",
        "line",
        "verdict",
        "confidence",
        "evidence"
      ]
    },
    "RuntimeCoverageHotPath": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable content-hash ID of the form `fallow:hot:<hash>`."
        },
        "path": {
          "type": "string"
        },
        "function": {
          "type": "string"
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "end_line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-indexed line the function ends on (inclusive). Mirrors\n`fallow_cov_protocol::HotPath::end_line` (added in protocol 0.5).\nOlder 0.4-shape sidecars omit the field on the wire; serde defaults\nto `0`, which the line-overlap filter MUST treat as a single-line\nrange (`line..=line`) rather than a span."
        },
        "invocations": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "percentile": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0,
          "maximum": 255,
          "description": "Percentile rank over this response's hot-path distribution. `100`\nmeans the busiest, `0` means the quietest function that qualified."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RuntimeCoverageAction"
          }
        }
      },
      "required": [
        "id",
        "path",
        "function",
        "line",
        "end_line",
        "invocations",
        "percentile"
      ]
    },
    "RuntimeCoverageRiskBand": {
      "type": "string",
      "enum": [
        "low",
        "medium",
        "high"
      ]
    },
    "RuntimeCoverageBlastRadiusEntry": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable content-hash ID of the form `fallow:blast:<hash>`."
        },
        "file": {
          "type": "string"
        },
        "function": {
          "type": "string"
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "caller_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "caller_count_weighted_by_traffic": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "deploys_touched": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0
        },
        "risk_band": {
          "$ref": "#/definitions/RuntimeCoverageRiskBand"
        }
      },
      "required": [
        "id",
        "file",
        "function",
        "line",
        "caller_count",
        "caller_count_weighted_by_traffic",
        "risk_band"
      ]
    },
    "RuntimeCoverageImportanceEntry": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string",
          "description": "Stable content-hash ID of the form `fallow:importance:<hash>`."
        },
        "file": {
          "type": "string"
        },
        "function": {
          "type": "string"
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "invocations": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0
        },
        "cyclomatic": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "owner_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0
        },
        "importance_score": {
          "type": "number",
          "format": "double"
        },
        "reason": {
          "type": "string"
        }
      },
      "required": [
        "id",
        "file",
        "function",
        "line",
        "invocations",
        "cyclomatic",
        "owner_count",
        "importance_score",
        "reason"
      ]
    },
    "DuplicationReport": {
      "type": "object",
      "properties": {
        "clone_groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneGroup"
          },
          "description": "All detected clone groups."
        },
        "clone_families": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneFamily"
          },
          "description": "Clone families: groups of clone groups sharing the same file set."
        },
        "mirrored_directories": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MirroredDirectory"
          },
          "description": "Detected mirrored directory trees (directories with many identical files)."
        },
        "stats": {
          "description": "Aggregate statistics.",
          "allOf": [
            {
              "$ref": "#/definitions/DuplicationStats"
            }
          ]
        }
      },
      "required": [
        "clone_groups",
        "clone_families",
        "stats"
      ],
      "description": "Overall duplication analysis report."
    },
    "HealthReport": {
      "type": "object",
      "properties": {
        "findings": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HealthFinding"
          },
          "description": "Functions exceeding thresholds."
        },
        "summary": {
          "description": "Summary statistics.",
          "allOf": [
            {
              "$ref": "#/definitions/HealthSummary"
            }
          ]
        },
        "vital_signs": {
          "anyOf": [
            {
              "$ref": "#/definitions/VitalSigns"
            },
            {
              "type": "null"
            }
          ],
          "description": "Project-wide vital signs (always computed from available data)."
        },
        "health_score": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthScore"
            },
            {
              "type": "null"
            }
          ],
          "description": "Project-wide health score (only populated with `--score`)."
        },
        "file_scores": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/FileHealthScore"
          },
          "description": "Per-file health scores (only populated with `--file-scores` or `--hotspots`)."
        },
        "coverage_gaps": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageGaps"
            },
            {
              "type": "null"
            }
          ],
          "description": "Static coverage gaps.\n\nPopulated when coverage gaps are explicitly requested, or when the\ntop-level `health` command allows config severity to surface them in the\ndefault report."
        },
        "hotspots": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HotspotEntry"
          },
          "description": "Hotspot entries (only populated with `--hotspots`)."
        },
        "hotspot_summary": {
          "anyOf": [
            {
              "$ref": "#/definitions/HotspotSummary"
            },
            {
              "type": "null"
            }
          ],
          "description": "Hotspot analysis summary (only set with `--hotspots`)."
        },
        "runtime_coverage": {
          "anyOf": [
            {
              "$ref": "#/definitions/RuntimeCoverageReport"
            },
            {
              "type": "null"
            }
          ],
          "description": "Runtime coverage findings from the paid sidecar (only populated with\n`--runtime-coverage`)."
        },
        "large_functions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/LargeFunctionEntry"
          },
          "description": "Functions exceeding 60 LOC (only populated when unit size very-high-risk >= 3%)."
        },
        "targets": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RefactoringTarget"
          },
          "description": "Ranked refactoring recommendations (only populated with `--targets`)."
        },
        "target_thresholds": {
          "anyOf": [
            {
              "$ref": "#/definitions/TargetThresholds"
            },
            {
              "type": "null"
            }
          ],
          "description": "Adaptive thresholds used for target scoring (only set with `--targets`)."
        },
        "health_trend": {
          "anyOf": [
            {
              "$ref": "#/definitions/HealthTrend"
            },
            {
              "type": "null"
            }
          ],
          "description": "Health trend comparison against a previous snapshot (only set with `--trend`)."
        }
      },
      "required": [
        "findings",
        "summary"
      ],
      "description": "Result of complexity analysis for reporting."
    },
    "DupesOutput": {
      "type": "object",
      "properties": {
        "schema_version": {
          "description": "Schema version for this output format.",
          "allOf": [
            {
              "$ref": "#/definitions/SchemaVersion"
            }
          ]
        },
        "version": {
          "description": "Fallow tool version that produced this output.",
          "allOf": [
            {
              "$ref": "#/definitions/ToolVersion"
            }
          ]
        },
        "elapsed_ms": {
          "description": "Analysis duration in milliseconds.",
          "allOf": [
            {
              "$ref": "#/definitions/ElapsedMs"
            }
          ]
        },
        "clone_groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneGroup"
          },
          "description": "All detected clone groups."
        },
        "clone_families": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneFamily"
          },
          "description": "Clone families: groups of clone groups sharing the same file set."
        },
        "mirrored_directories": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MirroredDirectory"
          },
          "description": "Detected mirrored directory trees (directories with many identical files)."
        },
        "stats": {
          "description": "Aggregate statistics.",
          "allOf": [
            {
              "$ref": "#/definitions/DuplicationStats"
            }
          ]
        },
        "grouped_by": {
          "anyOf": [
            {
              "$ref": "#/definitions/GroupByMode"
            },
            {
              "type": "null"
            }
          ],
          "description": "Resolver mode used for partitioning. Present only when `--group-by` is\nactive."
        },
        "total_issues": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "description": "Total clone groups across all buckets when `--group-by` is active.\nMirrors the grouped check / health envelopes which expose\n`total_issues` so MCP and CI consumers can read the same key across\ncommands."
        },
        "groups": {
          "description": "Per-group buckets when `--group-by` is active. Each clone group is\nattributed to its largest-owner key (most instances; alphabetical\ntiebreak)."
        },
        "_meta": {
          "anyOf": [
            {
              "$ref": "#/definitions/Meta"
            },
            {
              "type": "null"
            }
          ],
          "description": "`_meta` block with metric / rule definitions, emitted when `--explain`\nis passed (always present in MCP responses)."
        }
      },
      "required": [
        "schema_version",
        "version",
        "elapsed_ms",
        "clone_groups",
        "clone_families",
        "stats"
      ],
      "description": "Envelope emitted by `fallow dupes --format json` (plus the `dupes` block\ninside the combined and audit envelopes).\n\nThe body is the full `DuplicationReport` flattened into the envelope so\nthe wire shape stays `{ schema_version, version, elapsed_ms, clone_groups,\nclone_families, stats, ... }` exactly as the existing JSON layer emits.\n`grouped_by` / `groups` / `total_issues` are populated by the grouped\nbuilder; on the ungrouped path they stay `None` and `skip_serializing_if`\ndrops them."
    },
    "DuplicationGroup": {
      "type": "object",
      "description": "A single grouped duplication bucket. Per-group `stats` are dedup-aware and computed over the FULL group BEFORE any `--top` truncation.",
      "required": [
        "key",
        "stats",
        "clone_groups",
        "clone_families"
      ],
      "properties": {
        "key": {
          "type": "string",
          "description": "Group label (owner / directory / package / section). `(unowned)` for files with no CODEOWNERS rule, `(no section)` for pre-section rules in section mode."
        },
        "stats": {
          "$ref": "#/definitions/DuplicationStats"
        },
        "clone_groups": {
          "type": "array",
          "description": "Clone groups attributed to this owner. Each group's `primary_owner` is its largest-owner key; per-instance `owner` lets consumers see cross-bucket fan-out without re-resolving paths.",
          "items": {
            "$ref": "#/definitions/AttributedCloneGroup"
          }
        },
        "clone_families": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneFamily"
          }
        }
      }
    },
    "AttributedCloneGroup": {
      "type": "object",
      "description": "A clone group annotated with its largest-owner attribution and per-instance owner keys.",
      "required": [
        "primary_owner",
        "token_count",
        "line_count",
        "instances"
      ],
      "properties": {
        "primary_owner": {
          "type": "string",
          "description": "Largest-owner attribution: the resolver key with the most instances in this clone group. Ties broken alphabetically (smallest key wins)."
        },
        "token_count": {
          "type": "integer"
        },
        "line_count": {
          "type": "integer"
        },
        "instances": {
          "type": "array",
          "description": "Each instance carries its own `owner` field alongside the standard CloneInstance shape.",
          "items": {
            "type": "object",
            "required": [
              "file",
              "start_line",
              "end_line",
              "start_col",
              "end_col",
              "fragment",
              "owner"
            ],
            "properties": {
              "file": {
                "type": "string"
              },
              "start_line": {
                "type": "integer"
              },
              "end_line": {
                "type": "integer"
              },
              "start_col": {
                "type": "integer"
              },
              "end_col": {
                "type": "integer"
              },
              "fragment": {
                "type": "string"
              },
              "owner": {
                "type": "string",
                "description": "Resolver key for this specific instance (per-instance, not the group-level largest-owner)."
              }
            }
          }
        },
        "actions": {
          "type": "array",
          "description": "Suggested actions for this clone group (extract-shared, suppress-line, etc.)."
        }
      }
    },
    "SchemaVersion": {
      "type": "integer",
      "format": "uint32",
      "minimum": 0,
      "description": "Schema version for a fallow JSON envelope. Top-level fallow envelopes carry\na `schema_version` field whose Rust source is this newtype; the JSON wire\nshape is the bare integer.\n\nBump policy: ADDITIVE changes (new optional fields, new optional struct\nfields, new array entries, new MCP tools, new CLI flags) do NOT bump the\nversion; consumers receive new fields without breaking. BREAKING changes\n(renamed fields, removed fields, type changes, enum-variant removals,\nsemantic changes) DO bump. To detect newly-added fields without a bump,\ncheck field presence via JSON-key existence rather than gating on the\nversion."
    },
    "ToolVersion": {
      "type": "string",
      "description": "Fallow CLI version that produced this envelope. Renders to the JSON wire as\na bare string (e.g. `\"2.74.0\"`)."
    },
    "ElapsedMs": {
      "type": "integer",
      "format": "uint64",
      "minimum": 0,
      "description": "Analysis duration in milliseconds. Renders to the JSON wire as a bare\ninteger."
    },
    "HealthSummary": {
      "type": "object",
      "properties": {
        "files_analyzed": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files analyzed."
        },
        "functions_analyzed": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of functions found."
        },
        "functions_above_threshold": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of functions above threshold."
        },
        "max_cyclomatic_threshold": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "description": "Configured cyclomatic threshold."
        },
        "max_cognitive_threshold": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "description": "Configured cognitive threshold."
        },
        "max_crap_threshold": {
          "type": "number",
          "format": "double",
          "description": "Configured CRAP threshold. Functions meeting or exceeding this score\nare reported alongside complexity findings."
        },
        "files_scored": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "description": "Number of files scored (only set with `--file-scores`)."
        },
        "average_maintainability": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Average maintainability index across all scored files (only set with `--file-scores`)."
        },
        "coverage_model": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageModel"
            },
            {
              "type": "null"
            }
          ],
          "description": "Coverage model used for CRAP computation (None when file scores not computed)."
        },
        "istanbul_matched": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "description": "Number of functions matched against Istanbul coverage data.\nOnly present when `coverage_model` is `istanbul`."
        },
        "istanbul_total": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0,
          "description": "Total functions that could potentially be matched.\nOnly present when `coverage_model` is `istanbul`."
        },
        "severity_critical_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of findings with critical severity."
        },
        "severity_high_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of findings with high severity."
        },
        "severity_moderate_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of findings with moderate severity."
        }
      },
      "required": [
        "files_analyzed",
        "functions_analyzed",
        "functions_above_threshold",
        "max_cyclomatic_threshold",
        "max_cognitive_threshold",
        "max_crap_threshold",
        "severity_critical_count",
        "severity_high_count",
        "severity_moderate_count"
      ],
      "description": "Summary statistics for the health report."
    },
    "HealthFinding": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute file path."
        },
        "name": {
          "type": "string",
          "description": "Function name."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based column."
        },
        "cyclomatic": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "description": "Cyclomatic complexity."
        },
        "cognitive": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "description": "Cognitive complexity."
        },
        "line_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Number of lines in the function."
        },
        "param_count": {
          "type": "integer",
          "format": "uint8",
          "minimum": 0,
          "maximum": 255,
          "description": "Number of parameters."
        },
        "exceeded": {
          "description": "Which threshold was exceeded.",
          "allOf": [
            {
              "$ref": "#/definitions/ExceededThreshold"
            }
          ]
        },
        "severity": {
          "description": "How far above the threshold: moderate (just above), high, or critical.",
          "allOf": [
            {
              "$ref": "#/definitions/FindingSeverity"
            }
          ]
        },
        "crap": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "CRAP score (`CC^2 * (1 - cov/100)^3 + CC`), rounded to one decimal.\nPresent when the function also exceeded `--max-crap`, otherwise absent."
        },
        "coverage_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Per-function statement coverage percentage (0.0 to 100.0) used to\nderive `crap`. Present when Istanbul data matched the function,\notherwise absent (estimated model or unmatched functions)."
        },
        "coverage_tier": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageTier"
            },
            {
              "type": "null"
            }
          ],
          "description": "Bucketed coverage tier (`none`/`partial`/`high`) used to drive\naction selection in JSON output. Present whenever CRAP triggered\nthe finding (Istanbul or estimated), absent for findings that only\nexceeded cyclomatic/cognitive without CRAP context."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HealthFindingAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "path",
        "name",
        "line",
        "col",
        "cyclomatic",
        "cognitive",
        "line_count",
        "param_count",
        "exceeded",
        "severity",
        "actions"
      ],
      "description": "A single function that exceeds a complexity threshold."
    },
    "LargeFunctionEntry": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute file path."
        },
        "name": {
          "type": "string",
          "description": "Function name."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "line_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Number of lines in the function."
        }
      },
      "required": [
        "path",
        "name",
        "line",
        "line_count"
      ],
      "description": "A function exceeding the very-high-risk size threshold (>60 LOC)."
    },
    "FileHealthScore": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File path (absolute; stripped to relative in output)."
        },
        "fan_in": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files that import this file."
        },
        "fan_out": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files this file imports."
        },
        "dead_code_ratio": {
          "type": "number",
          "format": "double",
          "description": "Fraction of value exports with zero references (0.0–1.0). Files with no value exports get 0.0.\nType-only exports (interfaces, type aliases) are excluded from both numerator and denominator\nto avoid inflating the ratio for well-typed codebases that export props types alongside components."
        },
        "complexity_density": {
          "type": "number",
          "format": "double",
          "description": "Total cyclomatic complexity / lines of code."
        },
        "maintainability_index": {
          "type": "number",
          "format": "double",
          "description": "Weighted composite score (0–100, higher is better)."
        },
        "total_cyclomatic": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Sum of cyclomatic complexity across all functions."
        },
        "total_cognitive": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Sum of cognitive complexity across all functions."
        },
        "function_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of functions in this file."
        },
        "lines": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Total lines of code (from line_offsets)."
        },
        "crap_max": {
          "type": "number",
          "format": "double",
          "description": "Maximum CRAP score among functions in this file.\nComputed via the active `CoverageModel`: `StaticEstimated` (default,\ngraph-based per-function estimate), `Istanbul` (real per-function\nstatement coverage from `--coverage`), or the legacy `StaticBinary`.\nFormula: `CC^2 * (1 - cov/100)^3 + CC`."
        },
        "crap_above_threshold": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Count of functions with CRAP >= 30 (CC >= 5 without test path)."
        }
      },
      "required": [
        "path",
        "fan_in",
        "fan_out",
        "dead_code_ratio",
        "complexity_density",
        "maintainability_index",
        "total_cyclomatic",
        "total_cognitive",
        "function_count",
        "lines",
        "crap_max",
        "crap_above_threshold"
      ],
      "description": "Per-file health score combining complexity, coupling, and dead code metrics.\n\nFiles with zero functions (barrel files, re-export files) are excluded by default.\n\n## Maintainability Index Formula\n\n```text\ndampening = min(lines / 50, 1.0)\nfan_out_penalty = min(ln(fan_out + 1) × 4, 15)\nmaintainability = 100\n    - (complexity_density × 30 × dampening)\n    - (dead_code_ratio × 20)\n    - fan_out_penalty\n```\n\nClamped to \\[0, 100\\]. Higher is better. The dampening factor prevents\ncomplexity density from dominating the score on small files (< 50 lines).\n\n- **complexity_density**: total cyclomatic complexity / lines of code\n- **dead_code_ratio**: fraction of value exports (excluding type-only exports) with zero references (0.0–1.0)\n- **fan_out_penalty**: logarithmic scaling with cap at 15 points; reflects diminishing marginal risk of additional imports"
    },
    "HotspotEntry": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File path (absolute; stripped to relative in output)."
        },
        "score": {
          "type": "number",
          "format": "double",
          "description": "Hotspot score (0–100). Higher means more risk."
        },
        "commits": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Number of commits in the analysis window."
        },
        "weighted_commits": {
          "type": "number",
          "format": "double",
          "description": "Recency-weighted commit count (exponential decay, half-life 90 days)."
        },
        "lines_added": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Total lines added across all commits."
        },
        "lines_deleted": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Total lines deleted across all commits."
        },
        "complexity_density": {
          "type": "number",
          "format": "double",
          "description": "Cyclomatic complexity / lines of code."
        },
        "fan_in": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files that import this file (blast radius)."
        },
        "trend": {
          "description": "Churn trend: accelerating, stable, or cooling.",
          "allOf": [
            {
              "$ref": "#/definitions/ChurnTrend"
            }
          ]
        },
        "ownership": {
          "anyOf": [
            {
              "$ref": "#/definitions/OwnershipMetrics"
            },
            {
              "type": "null"
            }
          ],
          "description": "Ownership signals (bus factor, contributors, declared owner, drift).\nPopulated only when `--ownership` is requested."
        },
        "is_test_path": {
          "type": "boolean",
          "description": "True when the file path matches a test/mock convention (e.g.\n`**/__tests__/**`, `**/*.test.*`, `**/*.spec.*`, `**/__mocks__/**`).\nTest files are intentionally included in hotspot ranking (test\nmaintenance IS real work), but tagging them lets consumers decide\nwhether to weight or filter them downstream."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/HotspotAction"
          },
          "description": "Suggested actions to resolve this issue."
        }
      },
      "required": [
        "path",
        "score",
        "commits",
        "weighted_commits",
        "lines_added",
        "lines_deleted",
        "complexity_density",
        "fan_in",
        "trend",
        "actions"
      ],
      "description": "A hotspot: a file that is both complex and frequently changing.\n\n## Score Formula\n\n```text\nnormalized_churn = weighted_commits / max_weighted_commits   (0..1)\nnormalized_complexity = complexity_density / max_density      (0..1)\nscore = normalized_churn × normalized_complexity × 100       (0..100)\n```\n\nScore uses within-project max normalization. Higher score = higher risk.\nFan-in is shown separately as \"blast radius\" — not baked into the score."
    },
    "OwnershipMetrics": {
      "type": "object",
      "properties": {
        "bus_factor": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Avelino truck factor: minimum number of contributors covering at\nleast 50% of recency-weighted commits."
        },
        "contributor_count": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Distinct authors in the analysis window after bot filtering."
        },
        "top_contributor": {
          "description": "The highest-share contributor.",
          "allOf": [
            {
              "$ref": "#/definitions/ContributorEntry"
            }
          ]
        },
        "recent_contributors": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ContributorEntry"
          },
          "description": "Up to three additional contributors by share, ordered desc.\nUseful for \"who else could review this file\" routing."
        },
        "suggested_reviewers": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ContributorEntry"
          },
          "description": "Contributors whose last touch is within 90 days, ordered by share\ndescending. First-class field so AI agents do not have to\nreconstruct it from [`recent_contributors`](Self::recent_contributors)\nfiltered by [`ContributorEntry::stale_days`]. Excludes the top\ncontributor (they are the sole author being flagged); consumers\nwanting the full list can union with `top_contributor`."
        },
        "declared_owner": {
          "type": [
            "string",
            "null"
          ],
          "description": "CODEOWNERS-resolved owner for this file, if a rule matched.\nOnly the primary (first) owner of the matched rule is reported."
        },
        "unowned": {
          "type": [
            "boolean",
            "null"
          ],
          "description": "Tristate: `Some(true)` = CODEOWNERS file exists but no rule matches\nthis file; `Some(false)` = a CODEOWNERS rule matches; `None` = no\nCODEOWNERS file was discovered for the repository (cannot determine)."
        },
        "drift": {
          "type": "boolean",
          "description": "True when ownership has drifted from the original author to a new\ntop contributor. Pairs with [`drift_reason`](Self::drift_reason)."
        },
        "drift_reason": {
          "type": [
            "string",
            "null"
          ],
          "description": "Human-readable explanation of the drift, populated only when\n[`drift`](Self::drift) is true."
        }
      },
      "required": [
        "bus_factor",
        "contributor_count",
        "top_contributor",
        "drift"
      ],
      "description": "Per-file ownership signals attached to hotspot entries when the user\npasses `--ownership`. All fields are derived from git history and the\nrepository's CODEOWNERS file (if any)."
    },
    "ContributorEntry": {
      "type": "object",
      "properties": {
        "identifier": {
          "type": "string",
          "description": "Display string per the configured email mode: raw email\n(`alice@example.com`), local-part handle (`alice`), or stable hash\npseudonym (`xxh3:<16hex>`). The format depends on `format`.\n\nRenamed from `email` because in `handle` and `hash` modes the value\nis no longer an email address; consumers tempted to use it as one\n(e.g. `mailto:`) would be wrong."
        },
        "format": {
          "description": "Format of [`identifier`](Self::identifier): `raw`, `handle`, or `hash`.\nLets type-aware consumers branch without re-parsing the string.",
          "allOf": [
            {
              "$ref": "#/definitions/ContributorIdentifierFormat"
            }
          ]
        },
        "share": {
          "type": "number",
          "format": "double",
          "description": "Recency-weighted share of total weighted commits (0..1, three decimals)."
        },
        "stale_days": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "description": "Days since this contributor last touched the file."
        },
        "commits": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Total commits by this contributor in the analysis window."
        }
      },
      "required": [
        "identifier",
        "format",
        "share",
        "stale_days",
        "commits"
      ],
      "description": "Per-author summary emitted in [`OwnershipMetrics::top_contributor`] and\n[`OwnershipMetrics::recent_contributors`]."
    },
    "HotspotSummary": {
      "type": "object",
      "properties": {
        "since": {
          "type": "string",
          "description": "Analysis window display string (e.g., \"6 months\")."
        },
        "min_commits": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Minimum commits threshold."
        },
        "files_analyzed": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files with churn data meeting the threshold."
        },
        "files_excluded": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files excluded (below min_commits)."
        },
        "shallow_clone": {
          "type": "boolean",
          "description": "Whether the repository is a shallow clone."
        }
      },
      "required": [
        "since",
        "min_commits",
        "files_analyzed",
        "files_excluded",
        "shallow_clone"
      ],
      "description": "Summary statistics for hotspot analysis."
    },
    "RefactoringTarget": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute file path (stripped to relative in output)."
        },
        "priority": {
          "type": "number",
          "format": "double",
          "description": "Priority score (0–100, higher = more urgent)."
        },
        "efficiency": {
          "type": "number",
          "format": "double",
          "description": "Efficiency score (priority / effort). Higher = better quick-win value.\nSurfaces low-effort, high-priority targets first."
        },
        "recommendation": {
          "type": "string",
          "description": "One-line actionable recommendation."
        },
        "category": {
          "description": "Recommendation category for tooling/filtering.",
          "allOf": [
            {
              "$ref": "#/definitions/RecommendationCategory"
            }
          ]
        },
        "effort": {
          "description": "Estimated effort to address this target.",
          "allOf": [
            {
              "$ref": "#/definitions/EffortEstimate"
            }
          ]
        },
        "confidence": {
          "description": "Confidence in this recommendation based on data source reliability.",
          "allOf": [
            {
              "$ref": "#/definitions/Confidence"
            }
          ]
        },
        "factors": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ContributingFactor"
          },
          "description": "Which metric values contributed to this recommendation."
        },
        "evidence": {
          "anyOf": [
            {
              "$ref": "#/definitions/TargetEvidence"
            },
            {
              "type": "null"
            }
          ],
          "description": "Structured evidence linking to specific analysis data."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RefactoringTargetAction"
          },
          "description": "Suggested actions to resolve this issue."
        }
      },
      "required": [
        "path",
        "priority",
        "efficiency",
        "recommendation",
        "category",
        "effort",
        "confidence",
        "actions"
      ]
    },
    "TargetThresholds": {
      "type": "object",
      "properties": {
        "fan_in_p95": {
          "type": "number",
          "format": "double",
          "description": "Fan-in saturation point for priority formula (p95, floor 5)."
        },
        "fan_in_p75": {
          "type": "number",
          "format": "double",
          "description": "Fan-in moderate threshold for contributing factors (p75, floor 3)."
        },
        "fan_out_p95": {
          "type": "number",
          "format": "double",
          "description": "Fan-out saturation point for priority formula (p95, floor 8)."
        },
        "fan_out_p90": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Fan-out high threshold for rules and contributing factors (p90, floor 5)."
        }
      },
      "required": [
        "fan_in_p95",
        "fan_in_p75",
        "fan_out_p95",
        "fan_out_p90"
      ],
      "description": "Adaptive thresholds used for refactoring target scoring.\n\nDerived from the project's metric distribution (percentile-based with floors).\nExposed in JSON output so consumers can interpret scores in context."
    },
    "HealthTrend": {
      "type": "object",
      "properties": {
        "compared_to": {
          "description": "The snapshot being compared against.",
          "allOf": [
            {
              "$ref": "#/definitions/TrendPoint"
            }
          ]
        },
        "metrics": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TrendMetric"
          },
          "description": "Per-metric deltas."
        },
        "snapshots_loaded": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of snapshots found in the snapshot directory."
        },
        "overall_direction": {
          "description": "Overall direction across all metrics.",
          "allOf": [
            {
              "$ref": "#/definitions/TrendDirection"
            }
          ]
        }
      },
      "required": [
        "compared_to",
        "metrics",
        "snapshots_loaded",
        "overall_direction"
      ],
      "description": "Health trend comparison: current run vs. a previous snapshot."
    },
    "TrendCount": {
      "type": "object",
      "properties": {
        "value": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "The numerator (e.g., dead files count)."
        },
        "total": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "The denominator (e.g., total files)."
        }
      },
      "required": [
        "value",
        "total"
      ],
      "description": "Raw numerator/denominator for a percentage metric."
    },
    "CoverageGaps": {
      "type": "object",
      "properties": {
        "summary": {
          "description": "Summary metrics for the current analysis scope.",
          "allOf": [
            {
              "$ref": "#/definitions/CoverageGapSummary"
            }
          ]
        },
        "files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UntestedFile"
          },
          "description": "Runtime files with no test dependency path."
        },
        "exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UntestedExport"
          },
          "description": "Runtime exports with no test-reachable reference chain."
        }
      },
      "required": [
        "summary"
      ],
      "description": "Static test coverage gaps derived from the module graph."
    },
    "CoverageGapSummary": {
      "type": "object",
      "properties": {
        "runtime_files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Runtime-reachable files in scope."
        },
        "covered_files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Runtime-reachable files also reachable from tests."
        },
        "file_coverage_pct": {
          "type": "number",
          "format": "double",
          "description": "Percentage of runtime files that are test-reachable."
        },
        "untested_files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Runtime files with no test dependency path."
        },
        "untested_exports": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Runtime exports with no test-reachable reference chain."
        }
      },
      "required": [
        "runtime_files",
        "covered_files",
        "file_coverage_pct",
        "untested_files",
        "untested_exports"
      ],
      "description": "Aggregate coverage-gap counters for the current analysis scope."
    },
    "UntestedFile": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute file path."
        },
        "value_export_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of value exports declared by the file."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UntestedFileAction"
          },
          "description": "Suggested actions to resolve this issue."
        }
      },
      "required": [
        "path",
        "value_export_count",
        "actions"
      ],
      "description": "Runtime code that no test dependency path reaches."
    },
    "UntestedExport": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute file path."
        },
        "export_name": {
          "type": "string",
          "description": "Export name."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based source line."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based source column."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UntestedExportAction"
          },
          "description": "Suggested actions to resolve this issue."
        }
      },
      "required": [
        "path",
        "export_name",
        "line",
        "col",
        "actions"
      ],
      "description": "Runtime export that no test-reachable module references."
    },
    "UntestedFileAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/UntestedFileActionType"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether `fallow fix` can auto-apply this action. Today both\nvariants are manual."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional context for the `add-tests` variant (explains why no\ntest path reaches this file). Absent on `suppress-file`."
        },
        "comment": {
          "type": [
            "string",
            "null"
          ],
          "description": "The file-level comment to insert. Present on `suppress-file`\n(`// fallow-ignore-file coverage-gaps`). Absent on `add-tests`."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "description": "Suggested action attached to an [`UntestedFile`] coverage-gap finding.\n\n`inject_health_actions` emits a two-entry array on every untested-file\nitem: an `add-tests` primary action (scaffold tests for the runtime\nfile) and a `suppress-file` action (`// fallow-ignore-file coverage-gaps`).\nBoth variants share the same struct shape; the field that is populated\n(`note` for `add-tests`, `comment` for `suppress-file`) depends on the\n`kind`.\n\n[`UntestedFile`]: ../../fallow-cli/src/health_types/coverage.rs"
    },
    "UntestedExportAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/UntestedExportActionType"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether `fallow fix` can auto-apply this action. Today both\nvariants are manual."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional context for the `add-test-import` variant (explains the\nruntime-reachable / test-unreachable asymmetry). Absent on\n`suppress-file`."
        },
        "comment": {
          "type": [
            "string",
            "null"
          ],
          "description": "The file-level comment to insert. Present on `suppress-file`\n(`// fallow-ignore-file coverage-gaps`). Absent on\n`add-test-import`."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "description": "Suggested action attached to an [`UntestedExport`] coverage-gap\nfinding.\n\n`inject_health_actions` emits a two-entry array on every untested-export\nitem: an `add-test-import` primary action (import the export from a\ntest-reachable module) and a `suppress-file` action\n(`// fallow-ignore-file coverage-gaps`). The export-specific variant\n`add-test-import` reflects that a test-reachable reference chain, not\njust any test coverage, is what closes the gap.\n\n[`UntestedExport`]: ../../fallow-cli/src/health_types/coverage.rs"
    },
    "HealthScore": {
      "type": "object",
      "properties": {
        "formula_version": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Formula version used to compute the score and penalties."
        },
        "score": {
          "type": "number",
          "format": "double",
          "description": "Overall score (0–100, higher is better)."
        },
        "grade": {
          "type": "string",
          "description": "Letter grade: A, B, C, D, or F."
        },
        "penalties": {
          "description": "Per-component penalty breakdown. Shows what drove the score down.",
          "allOf": [
            {
              "$ref": "#/definitions/HealthScorePenalties"
            }
          ]
        }
      },
      "required": [
        "formula_version",
        "score",
        "grade",
        "penalties"
      ]
    },
    "HealthScorePenalties": {
      "type": "object",
      "properties": {
        "dead_files": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from dead files (max 15)."
        },
        "dead_exports": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from dead exports (max 15)."
        },
        "complexity": {
          "type": "number",
          "format": "double",
          "description": "Points lost from critical-complexity density (max 20)."
        },
        "p90_complexity": {
          "type": "number",
          "format": "double",
          "description": "Points lost from legacy p90 cyclomatic complexity (0 for current scale-invariant runs)."
        },
        "maintainability": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from low maintainability index density (max 15)."
        },
        "hotspots": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from hotspot files (max 10)."
        },
        "unused_deps": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from unused dependency density (max 25)."
        },
        "circular_deps": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from circular dependency density (max 25)."
        },
        "unit_size": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from oversized-function density (max 10)."
        },
        "coupling": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from coupling concentration density (max 5)."
        },
        "duplication": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Points lost from code duplication (max 10)."
        }
      },
      "required": [
        "complexity",
        "p90_complexity"
      ],
      "description": "Per-component penalty breakdown for the health score.\n\nEach field shows how many points were subtracted for that component.\n`None` means the metric was not available (pipeline didn't run)."
    },
    "VitalSigns": {
      "type": "object",
      "properties": {
        "dead_file_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Percentage of files not reachable from any entry point."
        },
        "dead_export_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Percentage of exports never imported by other modules."
        },
        "avg_cyclomatic": {
          "type": "number",
          "format": "double",
          "description": "Average cyclomatic complexity across all functions."
        },
        "critical_complexity_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Percentage of functions at or above the critical cyclomatic threshold."
        },
        "p90_cyclomatic": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "90th percentile cyclomatic complexity."
        },
        "duplication_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Code duplication percentage (None if duplication pipeline was not run)."
        },
        "hotspot_count": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "description": "Number of hotspot files (score >= 50). None if git history unavailable."
        },
        "hotspot_top_pct_count": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "description": "Number of files in the top 1% of the within-project hotspot ranking."
        },
        "maintainability_avg": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Average maintainability index across all scored files (0–100)."
        },
        "maintainability_low_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Percentage of scored files with maintainability index below 70."
        },
        "unused_dep_count": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "description": "Number of unused dependencies (dependencies + devDependencies + optional)."
        },
        "unused_deps_per_k_files": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Unused dependencies per 1,000 files."
        },
        "circular_dep_count": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "description": "Number of circular dependency chains."
        },
        "circular_deps_per_k_files": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Circular dependency chains per 1,000 files."
        },
        "counts": {
          "anyOf": [
            {
              "$ref": "#/definitions/VitalSignsCounts"
            },
            {
              "type": "null"
            }
          ],
          "description": "Raw counts backing the percentages (for orientation header display)."
        },
        "unit_size_profile": {
          "anyOf": [
            {
              "$ref": "#/definitions/RiskProfile"
            },
            {
              "type": "null"
            }
          ],
          "description": "Function size risk profile: percentage of functions in each size bin."
        },
        "functions_over_60_loc_per_k": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Functions above 60 LOC per 1,000 functions."
        },
        "unit_interfacing_profile": {
          "anyOf": [
            {
              "$ref": "#/definitions/RiskProfile"
            },
            {
              "type": "null"
            }
          ],
          "description": "Parameter count risk profile: percentage of functions in each param bin."
        },
        "p95_fan_in": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "description": "95th percentile fan-in across all files."
        },
        "coupling_high_pct": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Percentage of files with fan-in above the project's p95 threshold."
        },
        "total_loc": {
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "description": "Total lines of code across all parsed modules.",
          "default": 0
        }
      },
      "required": [
        "avg_cyclomatic",
        "p90_cyclomatic"
      ],
      "description": "Project-wide vital signs — a fixed set of metrics for trend tracking.\n\nMetrics are `Option` when the data source was not available in the current run\n(e.g., `duplication_pct` is `None` unless the duplication pipeline was run,\n`hotspot_count` is `None` without git history)."
    },
    "RiskProfile": {
      "type": "object",
      "properties": {
        "low_risk": {
          "type": "number",
          "format": "double",
          "description": "Percentage of functions in the low-risk bin."
        },
        "medium_risk": {
          "type": "number",
          "format": "double",
          "description": "Percentage of functions in the medium-risk bin."
        },
        "high_risk": {
          "type": "number",
          "format": "double",
          "description": "Percentage of functions in the high-risk bin."
        },
        "very_high_risk": {
          "type": "number",
          "format": "double",
          "description": "Percentage of functions in the very-high-risk bin."
        }
      },
      "required": [
        "low_risk",
        "medium_risk",
        "high_risk",
        "very_high_risk"
      ],
      "description": "Risk profile: percentage of functions in each risk bin.\n\nBins are defined by thresholds that depend on the measured property:\n- **Unit size**: low risk (1-15 LOC), medium risk (16-30), high risk (31-60), very high risk (>60)\n- **Unit interfacing**: low risk (0-2 params), medium risk (3-4), high risk (5-6), very high risk (>=7)\n\nPercentages sum to approximately 100.0 (subject to rounding)."
    },
    "IssueAction": {
      "anyOf": [
        {
          "description": "A code-change fix the user can apply (auto-fixable by `fallow fix` for\nsome variants, manual for others).",
          "allOf": [
            {
              "$ref": "#/definitions/FixAction"
            }
          ]
        },
        {
          "description": "Place a `// fallow-ignore-next-line ...` comment above the offending\nline. Always manual.",
          "allOf": [
            {
              "$ref": "#/definitions/SuppressLineAction"
            }
          ]
        },
        {
          "description": "Place a `// fallow-ignore-file ...` comment at the top of the file.\nAlways manual.",
          "allOf": [
            {
              "$ref": "#/definitions/SuppressFileAction"
            }
          ]
        },
        {
          "description": "Add the offending finding to the fallow config (e.g.\n`ignoreDependencies: [\"lodash\"]`). Auto-fixable for the array-shaped\n`ignoreExports` variant when a config file exists; manual otherwise.",
          "allOf": [
            {
              "$ref": "#/definitions/AddToConfigAction"
            }
          ]
        }
      ],
      "description": "A suggested action attached to a finding in the JSON output. Each finding\ncarries an `actions` array; consumers (agents, IDE clients, CI bots) can\ndispatch on the `type` discriminant to choose the right remediation.\n\nThe discriminator is `type` (snake_case `type` field), the payload uses the\nmatching kebab-case identifier per variant."
    },
    "FixAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Kebab-case identifier for the fix action.",
          "allOf": [
            {
              "$ref": "#/definitions/FixActionType"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether `fallow fix` can apply this fix automatically."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the fix."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional context note. Present on non-auto-fixable actions, and on\nauto-fixable re-export findings to warn about public API surface."
        },
        "available_in_catalogs": {
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "description": "Only present on `update-catalog-reference` actions: catalogs in the\nsame workspace that DO declare the package, sorted lexicographically.\nLets agents pick the catalog to switch to without re-reading the\nsource."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "description": "A code-change fix. `type` is one of the kebab-case identifiers in\n[`FixActionType`]."
    },
    "SuppressLineAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/SuppressLineKind"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Always false for suppress actions."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the suppression."
        },
        "comment": {
          "type": "string",
          "description": "The inline comment to place above the line (e.g.,\n`// fallow-ignore-next-line unused-export`). When multiple\nsuppressible findings share the same path and line, this may contain a\ncomma-separated issue-kind list such as\n`// fallow-ignore-next-line unused-export, complexity`."
        },
        "scope": {
          "anyOf": [
            {
              "$ref": "#/definitions/SuppressLineScope"
            },
            {
              "type": "null"
            }
          ],
          "description": "Present on multi-location issue types (e.g., `duplicate_exports`) to\nindicate the comment must be applied at each location."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description",
        "comment"
      ],
      "description": "Inline-comment suppression for a single finding line."
    },
    "SuppressFileAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/SuppressFileKind"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Always false for suppress actions."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the suppression."
        },
        "comment": {
          "type": "string",
          "description": "The file-level comment to place at the top of the file (e.g.,\n`// fallow-ignore-file unused-file`)."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description",
        "comment"
      ],
      "description": "File-wide suppression placed at the top of the source file."
    },
    "AddToConfigAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/AddToConfigKind"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "True when `fallow fix` can apply this config action automatically for\nthe current action type. `ignoreExports` duplicate-export actions are\nauto-fixable when a config file exists; older scalar config-ignore\nactions remain manual."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the config change."
        },
        "config_key": {
          "type": "string",
          "description": "The fallow config key to add the value to (e.g.,\n`ignoreDependencies`)."
        },
        "value": {
          "description": "Value to add to the config key. Shape depends on `config_key`. For\nscalar config keys (`ignoreDependencies`, others) this is a string\nsuch as `\"lodash\"`. For `ignoreExports` this is an array of\n`{ file, exports }` rule objects so the snippet can be merged into\nthe user's config verbatim. For `ignoreCatalogReferences` and\n`ignoreDependencyOverrides` this is an object whose shape matches the\nrule entry users add to their fallow config.",
          "allOf": [
            {
              "$ref": "#/definitions/AddToConfigValue"
            }
          ]
        },
        "value_schema": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional URL pointing at a stable JSON Schema fragment that describes\nthe shape of `value`. Agents that intend to validate `value` before\nwriting it into a user's config can fetch the linked schema and run\nit against `value`. The URL is a JSON Pointer fragment into fallow's\nmain config schema (e.g.\n`schema.json#/properties/ignoreExports` for the ignoreExports\naction, or `schema.json#/properties/ignoreDependencies/items` for\nthe per-package ignoreDependencies action). Strictly additive:\nconsumers that ignore the field keep working unchanged."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description",
        "config_key",
        "value"
      ],
      "description": "Edit a fallow config file (`.fallowrc.json`, `fallow.toml`, etc.) to\nadd the offending value to an `ignore*` rule."
    },
    "HealthFindingAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier. Kebab-case per the JSON output contract.",
          "allOf": [
            {
              "$ref": "#/definitions/HealthFindingActionType"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether `fallow fix` can auto-apply this action. Today every health\nfinding action is manual, but the field is non-singleton so a future\nauto-applier (e.g., an LLM-driven `refactor-function` worker) does\nnot need a schema change."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional context (e.g., the canonical CRAP formula, or a hint\nabout which branch type to extract). Present on most action types;\ndropped only when the description carries the full ask."
        },
        "comment": {
          "type": [
            "string",
            "null"
          ],
          "description": "The inline comment to insert (e.g.,\n`// fallow-ignore-next-line complexity` or\n`<!-- fallow-ignore-file complexity -->`). Present on\n`suppress-line` and `suppress-file` action variants."
        },
        "placement": {
          "type": [
            "string",
            "null"
          ],
          "description": "Where to insert the suppress comment\n(e.g., `above-function-declaration`, `above-angular-decorator`, or\n`top-of-template`). Present on `suppress-line` and `suppress-file`\naction variants."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "description": "Suggested action attached to a [`HealthFinding`].\n\nEach complexity finding carries an array of these on the JSON wire\n(`findings[].actions[]`). The action selector in\n`crates/cli/src/report/json.rs::build_health_finding_actions` picks the\nprimary action based on which thresholds triggered the finding and the\nbucketed coverage tier. See [`HealthFindingActionType`] for the full\ndiscriminant list.\n\n`note`, `comment`, and `placement` are populated per-variant: refactor\nactions carry a `note`, suppress-line / suppress-file actions carry\n`comment` plus `placement`, and the coverage-leaning actions\n(`add-tests`, `increase-coverage`) carry only `note`.\n\n[`HealthFinding`]: ../../fallow-cli/src/health_types/scores.rs"
    },
    "HotspotAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/HotspotActionType"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether `fallow fix` can auto-apply this action. Today every\nhotspot action is manual."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action."
        },
        "note": {
          "type": [
            "string",
            "null"
          ],
          "description": "Additional context for the action. Absent on `low-bus-factor` when\nthe finding's description already carries the full ask (no\nsuggested reviewers and not a low-commit file)."
        },
        "suggested_pattern": {
          "type": [
            "string",
            "null"
          ],
          "description": "Suggested CODEOWNERS pattern. Present only on `unowned-hotspot`\nactions. Derived per the [`heuristic`](Self::heuristic) field;\nconsumers should branch on [`heuristic`](Self::heuristic) rather\nthan assume a stable algorithm."
        },
        "heuristic": {
          "anyOf": [
            {
              "$ref": "#/definitions/HotspotActionHeuristic"
            },
            {
              "type": "null"
            }
          ],
          "description": "Strategy used to derive [`suggested_pattern`](Self::suggested_pattern).\nReserved for future evolution (`codeowners-cluster`, etc.)."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "description": "Suggested action attached to a [`HotspotEntry`].\n\nThe action list always begins with `refactor-file` plus `add-tests`.\nOwnership-derived variants (`low-bus-factor`, `unowned-hotspot`,\n`ownership-drift`) are appended only when `--ownership` is active AND\nthe corresponding signal fires for the hotspot.\n\n[`HotspotEntry`]: ../../fallow-cli/src/health_types/scores.rs"
    },
    "RefactoringTargetAction": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Action type identifier.",
          "allOf": [
            {
              "$ref": "#/definitions/RefactoringTargetActionType"
            }
          ]
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether `fallow fix` can auto-apply this action. Today both\nvariants are manual."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action. For `apply-refactoring`\nthis is the target's own `recommendation` string; for\n`suppress-line` it is the suppression prompt."
        },
        "category": {
          "type": [
            "string",
            "null"
          ],
          "description": "Recommendation category for `apply-refactoring` actions. Mirrors\nthe parent target's\n[`category`](../../fallow-cli/src/health_types/targets.rs.html)\nfield so consumers can route on the action alone."
        },
        "comment": {
          "type": [
            "string",
            "null"
          ],
          "description": "The inline comment to insert. Present on `suppress-line` actions\nwhen evidence exists."
        }
      },
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "description": "Suggested action attached to a [`RefactoringTarget`].\n\nThe list always begins with `apply-refactoring`. A trailing\n`suppress-line` is appended only when the target carries `evidence`\nlinking to specific functions (e.g., `extract_complex_functions`,\n`add_test_coverage`).\n\nUnlike [`HealthFindingAction`], the `suppress-line` variant emitted\nhere does NOT carry a `placement` field: the parent\n[`RefactoringTarget`] points at a file (not a specific function\ndeclaration site), so a per-line placement hint would have no\nreferent. Consumers that want the placement metadata should follow\nthe target's `evidence.complex_functions` back to the matching\n[`HealthFinding`] and read placement from THAT action instead.\n\n[`RefactoringTarget`]: ../../fallow-cli/src/health_types/targets.rs"
    },
    "CloneFamilyAction": {
      "description": "A suggested action for a clone family.",
      "type": "object",
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "extract-shared",
            "apply-suggestion",
            "suppress-line"
          ],
          "description": "Action type identifier."
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether fallow can auto-fix this action."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action."
        },
        "note": {
          "type": "string",
          "description": "Additional context for the action."
        },
        "comment": {
          "type": "string",
          "description": "The inline comment to insert (e.g., '// fallow-ignore-next-line code-duplication'). Present for suppress-line actions."
        }
      }
    },
    "CloneGroupAction": {
      "description": "A suggested action for a clone group.",
      "type": "object",
      "required": [
        "type",
        "auto_fixable",
        "description"
      ],
      "properties": {
        "type": {
          "type": "string",
          "enum": [
            "extract-shared",
            "suppress-line"
          ],
          "description": "Action type identifier."
        },
        "auto_fixable": {
          "type": "boolean",
          "description": "Whether fallow can auto-fix this action."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the action."
        },
        "comment": {
          "type": "string",
          "description": "The inline comment to insert (e.g., '// fallow-ignore-next-line code-duplication'). Present for suppress-line actions."
        }
      }
    },
    "UnusedFile": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "Absolute path to the unused file."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "path",
        "actions"
      ],
      "description": "A file that is not reachable from any entry point."
    },
    "UnusedExport": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the unused export."
        },
        "export_name": {
          "type": "string",
          "description": "Name of the unused export."
        },
        "is_type_only": {
          "type": "boolean",
          "description": "Whether this is a type-only export."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the export."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        },
        "span_start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Byte offset into the source file (used by the fix command)."
        },
        "is_re_export": {
          "type": "boolean",
          "description": "Whether this finding comes from a barrel/index re-export rather than the source definition."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "path",
        "export_name",
        "is_type_only",
        "line",
        "col",
        "span_start",
        "is_re_export",
        "actions"
      ],
      "description": "An export that is never imported by other modules."
    },
    "PrivateTypeLeak": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the exported symbol."
        },
        "export_name": {
          "type": "string",
          "description": "Export whose public signature leaks the private type."
        },
        "type_name": {
          "type": "string",
          "description": "Private type referenced by the public signature."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the leaking type reference."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        },
        "span_start": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Byte offset of the type reference."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "path",
        "export_name",
        "type_name",
        "line",
        "col",
        "span_start",
        "actions"
      ],
      "description": "A public export signature that references a same-file private type."
    },
    "UnusedDependency": {
      "type": "object",
      "properties": {
        "package_name": {
          "type": "string",
          "description": "Package name, including internal workspace package names."
        },
        "location": {
          "description": "Whether this is in `dependencies`, `devDependencies`, or `optionalDependencies`.",
          "allOf": [
            {
              "$ref": "#/definitions/DependencyLocation"
            }
          ]
        },
        "path": {
          "type": "string",
          "description": "Path to the package.json where this dependency is listed.\nFor root deps this is `<root>/package.json`, for workspace deps it is `<ws>/package.json`."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the dependency entry in package.json."
        },
        "used_in_workspaces": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Workspace roots that import this package even though the declaring workspace does not."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "package_name",
        "location",
        "path",
        "line",
        "actions"
      ],
      "description": "A dependency that is listed in package.json but never imported."
    },
    "UnusedMember": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the unused member."
        },
        "parent_name": {
          "type": "string",
          "description": "Name of the parent enum or class."
        },
        "member_name": {
          "type": "string",
          "description": "Name of the unused member."
        },
        "kind": {
          "description": "Whether this is an enum member, class method, or class property.",
          "allOf": [
            {
              "$ref": "#/definitions/MemberKind"
            }
          ]
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "path",
        "parent_name",
        "member_name",
        "kind",
        "line",
        "col",
        "actions"
      ],
      "description": "An unused enum or class member."
    },
    "UnresolvedImport": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the unresolved import."
        },
        "specifier": {
          "type": "string",
          "description": "The import specifier that could not be resolved."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset of the import statement."
        },
        "specifier_col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset of the source string literal (the specifier in quotes).\nUsed by the LSP to underline just the specifier, not the entire import line."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "path",
        "specifier",
        "line",
        "col",
        "specifier_col",
        "actions"
      ],
      "description": "An import that could not be resolved."
    },
    "UnlistedDependency": {
      "type": "object",
      "properties": {
        "package_name": {
          "type": "string",
          "description": "Package name, including internal workspace package names."
        },
        "imported_from": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ImportSite"
          },
          "description": "Import sites where this unlisted dependency is used (file path, line, column)."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "package_name",
        "imported_from",
        "actions"
      ],
      "description": "A dependency used in code but not listed in package.json."
    },
    "ImportSite": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the import."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        }
      },
      "required": [
        "path",
        "line",
        "col"
      ],
      "description": "A location where an import occurs."
    },
    "DuplicateExport": {
      "type": "object",
      "properties": {
        "export_name": {
          "type": "string",
          "description": "The duplicated export name."
        },
        "locations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DuplicateLocation"
          },
          "description": "Locations where this export name appears."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "export_name",
        "locations",
        "actions"
      ],
      "description": "An export that appears multiple times across the project."
    },
    "DuplicateLocation": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the duplicate export."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        }
      },
      "required": [
        "path",
        "line",
        "col"
      ],
      "description": "A location where a duplicate export appears."
    },
    "TypeOnlyDependency": {
      "type": "object",
      "properties": {
        "package_name": {
          "type": "string",
          "description": "npm package name."
        },
        "path": {
          "type": "string",
          "description": "Path to the package.json where the dependency is listed."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the dependency entry in package.json."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "package_name",
        "path",
        "line",
        "actions"
      ],
      "description": "A production dependency that is only used via type-only imports.\nIn production builds, type imports are erased, so this dependency\nis not needed at runtime and could be moved to devDependencies."
    },
    "TestOnlyDependency": {
      "type": "object",
      "properties": {
        "package_name": {
          "type": "string",
          "description": "npm package name."
        },
        "path": {
          "type": "string",
          "description": "Path to the package.json where the dependency is listed."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the dependency entry in package.json."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "package_name",
        "path",
        "line",
        "actions"
      ],
      "description": "A production dependency that is only imported by test files.\nSince it is never used in production code, it could be moved to devDependencies."
    },
    "CircularDependency": {
      "type": "object",
      "properties": {
        "files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Files forming the cycle, in import order."
        },
        "length": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files in the cycle."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the import that starts the cycle (in the first file).",
          "default": 0
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset of the import that starts the cycle.",
          "default": 0
        },
        "is_cross_package": {
          "type": "boolean",
          "description": "Whether this cycle crosses workspace package boundaries."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "files",
        "length",
        "line",
        "col",
        "actions"
      ],
      "description": "A circular dependency chain detected in the module graph.\n\nThe `line` and `col` fields carry `#[serde(default)]` so callers reading\nhistorical baseline JSON without these fields can still deserialize the\nstruct, but the JSON output layer always emits them (u32 always\nserializes, never via `skip_serializing_if`). The schemars derive sees\nthe serde defaults and marks both fields optional in the generated\nschema; the explicit `extend(\"required\" = ...)` override here keeps the\nschema's `required` array honest about what the JSON output actually\ncontains."
    },
    "BoundaryViolation": {
      "type": "object",
      "properties": {
        "from_path": {
          "type": "string",
          "description": "The file making the disallowed import."
        },
        "to_path": {
          "type": "string",
          "description": "The file being imported that violates the boundary."
        },
        "from_zone": {
          "type": "string",
          "description": "The zone the importing file belongs to."
        },
        "to_zone": {
          "type": "string",
          "description": "The zone the imported file belongs to."
        },
        "import_specifier": {
          "type": "string",
          "description": "The raw import specifier from the source file."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the import statement in the source file."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset of the import statement."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "from_path",
        "to_path",
        "from_zone",
        "to_zone",
        "import_specifier",
        "line",
        "col",
        "actions"
      ],
      "description": "An import that crosses an architecture boundary rule."
    },
    "StaleSuppression": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the stale suppression."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the suppression comment or tag."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        },
        "origin": {
          "description": "The origin and details of the stale suppression.",
          "allOf": [
            {
              "$ref": "#/definitions/SuppressionOrigin"
            }
          ]
        }
      },
      "required": [
        "path",
        "line",
        "col",
        "origin"
      ],
      "description": "A suppression comment or JSDoc tag that no longer matches any issue."
    },
    "UnusedCatalogEntry": {
      "type": "object",
      "properties": {
        "entry_name": {
          "type": "string",
          "description": "Package name declared in the catalog (e.g. `\"react\"`, `\"@scope/lib\"`)."
        },
        "catalog_name": {
          "type": "string",
          "description": "Catalog group: `\"default\"` for the top-level `catalog:` map, or the\nnamed catalog key for entries declared under `catalogs.<name>:`."
        },
        "path": {
          "type": "string",
          "description": "Path to `pnpm-workspace.yaml`, relative to the analyzed root."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the catalog entry within `pnpm-workspace.yaml`."
        },
        "hardcoded_consumers": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Workspace `package.json` files that declare the same package with a\nhardcoded version range instead of `catalog:`. Empty when no consumer\nuses a hardcoded version. Sorted lexicographically for deterministic\noutput."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "entry_name",
        "catalog_name",
        "path",
        "line",
        "actions"
      ],
      "description": "A pnpm catalog entry declared in pnpm-workspace.yaml that no workspace package\nreferences via the `catalog:` protocol.\n\nThe default catalog (top-level `catalog:` key) uses `catalog_name: \"default\"`.\nNamed catalogs (under `catalogs.<name>:`) use their declared name."
    },
    "EmptyCatalogGroup": {
      "type": "object",
      "properties": {
        "catalog_name": {
          "type": "string",
          "description": "Catalog group name declared under the top-level `catalogs:` map."
        },
        "path": {
          "type": "string",
          "description": "Path to `pnpm-workspace.yaml`, relative to the analyzed root."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the empty group header within `pnpm-workspace.yaml`."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "catalog_name",
        "path",
        "line",
        "actions"
      ],
      "description": "A named `catalogs.<name>:` group in `pnpm-workspace.yaml` with no package entries."
    },
    "UnresolvedCatalogReference": {
      "type": "object",
      "properties": {
        "entry_name": {
          "type": "string",
          "description": "Package name being referenced via the catalog protocol (e.g. `\"react\"`)."
        },
        "catalog_name": {
          "type": "string",
          "description": "Catalog group the reference points at: `\"default\"` for bare `catalog:` references,\nor the named catalog key for `catalog:<name>` references."
        },
        "path": {
          "type": "string",
          "description": "Absolute path to the consumer `package.json`. Matches the storage\nconvention used by every path-anchored finding type (`UnusedFile`,\n`UnresolvedImport`, `UnusedExport`, etc.) so the shared filtering\npipelines (`filter_results_by_changed_files`, per-file overrides,\naudit attribution) work without a separate root-join pass. JSON\noutput strips the project-root prefix via `serde_path::serialize`."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the dependency entry in the consumer `package.json`."
        },
        "available_in_catalogs": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Other catalogs (in the same `pnpm-workspace.yaml`) that DO declare this\npackage. Empty when no catalog has the package. Sorted lexicographically.\nLets agents and humans decide whether to switch the reference to a\ndifferent catalog or to add the entry to the named catalog."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "entry_name",
        "catalog_name",
        "path",
        "line",
        "actions"
      ],
      "description": "A workspace package.json reference (`catalog:` or `catalog:<name>`) that points\nat a catalog which does not declare the consumed package.\n\n`pnpm install` errors at install time with `ERR_PNPM_CATALOG_ENTRY_NOT_FOUND_FOR_CATALOG_PROTOCOL`\nwhen this happens. fallow surfaces it statically so the failure is caught at\n`fallow check` time, before any install.\n\nThe default catalog (bare `catalog:` references the top-level `catalog:` map)\nuses `catalog_name: \"default\"`. Named catalogs (`catalog:react17`) use the\ndeclared catalog name."
    },
    "UnusedDependencyOverride": {
      "type": "object",
      "properties": {
        "raw_key": {
          "type": "string",
          "description": "The full original override key as written in the source (e.g.\n`\"react>react-dom\"`, `\"@types/react@<18\"`). Preserved for round-trip\nreporting so agents see the unmodified spelling."
        },
        "target_package": {
          "type": "string",
          "description": "The target package the override rewrites (e.g. `\"react-dom\"` for\n`\"react>react-dom\"`, `\"@types/react\"` for `\"@types/react@<18\"`)."
        },
        "parent_package": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional parent package (left side of `>`). `None` for bare-target keys."
        },
        "version_constraint": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional version selector on the target (e.g. `Some(\"<18\")` for\n`\"@types/react@<18\"`)."
        },
        "version_range": {
          "type": "string",
          "description": "The right-hand side of the entry: the version pnpm should force."
        },
        "source": {
          "description": "Where the override entry was declared.",
          "allOf": [
            {
              "$ref": "#/definitions/DependencyOverrideSource"
            }
          ]
        },
        "path": {
          "type": "string",
          "description": "Path to the source file. `pnpm-workspace.yaml` or a `package.json`,\nstored as an absolute filesystem path so `--changed-since` and\nper-file `overrides.rules` can compare directly against the analyzer's\nchanged-set / per-path rule lookups. JSON serialization strips the\nproject root via `serde_path::serialize`, matching the\n`UnresolvedCatalogReference` convention."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the entry within the source file."
        },
        "hint": {
          "type": [
            "string",
            "null"
          ],
          "description": "Soft hint reminding consumers to verify the override before removal.\nEmitted on every unused-override finding (both bare-target and\nparent-chain shapes) because projects without a readable lockfile still\nuse the conservative package-manifest fallback."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "raw_key",
        "target_package",
        "version_range",
        "source",
        "path",
        "line",
        "actions"
      ],
      "description": "An entry in pnpm's `overrides:` map (or the legacy `pnpm.overrides` in\n`package.json`) whose target package is not declared in any workspace\n`package.json` and is not present in `pnpm-lock.yaml`. Projects without a\nreadable lockfile fall back to package manifest checks; the `hint` field\nflags that conservative mode."
    },
    "MisconfiguredDependencyOverride": {
      "type": "object",
      "properties": {
        "raw_key": {
          "type": "string",
          "description": "The full original override key as written in the source."
        },
        "target_package": {
          "type": [
            "string",
            "null"
          ],
          "description": "Parsed target package name when the key was syntactically valid (the\n`EmptyValue` reason path). `None` for `UnparsableKey` findings whose\nkey could not be parsed at all. Used by JSON `add-to-config` actions to\nemit a paste-ready `ignoreDependencyOverrides` value that matches the\nsuppression matcher (which also keys on `target_package`); avoids the\npitfall where `raw_key` like `\"react@<18\"` would not match the rule\nthat targets package `\"react\"`."
        },
        "raw_value": {
          "type": "string",
          "description": "The right-hand side of the entry, exactly as written. Empty when the\nvalue was missing."
        },
        "reason": {
          "description": "Classifier for the misconfiguration.",
          "allOf": [
            {
              "$ref": "#/definitions/DependencyOverrideMisconfigReason"
            }
          ]
        },
        "source": {
          "description": "Where the override entry was declared.",
          "allOf": [
            {
              "$ref": "#/definitions/DependencyOverrideSource"
            }
          ]
        },
        "path": {
          "type": "string",
          "description": "Path to the source file. Stored as an absolute filesystem path so\n`--changed-since` and per-file `overrides.rules` can compare directly.\nJSON serialization strips the project root via `serde_path::serialize`."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number of the entry within the source file."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IssueAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "raw_key",
        "raw_value",
        "reason",
        "source",
        "path",
        "line",
        "actions"
      ],
      "description": "An override entry whose key or value is malformed. Default severity is\n`error` because pnpm refuses to install (or silently produces a no-op\noverride) when it encounters these shapes."
    },
    "CloneGroup": {
      "type": "object",
      "properties": {
        "instances": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneInstance"
          },
          "description": "All instances where this duplicated code appears."
        },
        "token_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of tokens in the duplicated block."
        },
        "line_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of lines in the duplicated block."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneGroupAction"
          },
          "description": "Suggested actions to resolve this issue."
        },
        "introduced": {
          "$ref": "#/definitions/AuditIntroduced"
        }
      },
      "required": [
        "instances",
        "token_count",
        "line_count",
        "actions"
      ],
      "description": "A group of code clones -- the same (or normalized-equivalent) code appearing in multiple places."
    },
    "CloneInstance": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "description": "Path to the file containing this clone instance."
        },
        "start_line": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "1-based start line of the clone."
        },
        "end_line": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "1-based end line of the clone."
        },
        "start_col": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "0-based start column."
        },
        "end_col": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "0-based end column."
        },
        "fragment": {
          "type": "string",
          "description": "The actual source code fragment."
        }
      },
      "required": [
        "file",
        "start_line",
        "end_line",
        "start_col",
        "end_col",
        "fragment"
      ],
      "description": "A single instance of duplicated code at a specific location."
    },
    "CloneFamily": {
      "type": "object",
      "properties": {
        "files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "The files involved in this family (sorted for stable output)."
        },
        "groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneGroup"
          },
          "description": "Clone groups belonging to this family."
        },
        "total_duplicated_lines": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of duplicated lines across all groups."
        },
        "total_duplicated_tokens": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of duplicated tokens across all groups."
        },
        "suggestions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/RefactoringSuggestion"
          },
          "description": "Refactoring suggestions for this family."
        },
        "actions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CloneFamilyAction"
          },
          "description": "Suggested actions to resolve this issue."
        }
      },
      "required": [
        "files",
        "groups",
        "total_duplicated_lines",
        "total_duplicated_tokens",
        "suggestions",
        "actions"
      ],
      "description": "A clone family: a set of clone groups that share the same file set.\n\nWhen multiple clone groups are all duplicated between the same set of files,\nthey form a family — indicating a deeper structural relationship that should\nbe refactored together rather than group-by-group."
    },
    "RefactoringSuggestion": {
      "type": "object",
      "required": [
        "kind",
        "description",
        "estimated_savings"
      ],
      "properties": {
        "kind": {
          "type": "string",
          "enum": [
            "ExtractFunction",
            "ExtractModule"
          ],
          "description": "What kind of refactoring is suggested."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the suggestion."
        },
        "estimated_savings": {
          "type": "integer",
          "description": "Estimated lines that could be eliminated."
        }
      }
    },
    "DuplicationStats": {
      "type": "object",
      "required": [
        "total_files",
        "files_with_clones",
        "total_lines",
        "duplicated_lines",
        "total_tokens",
        "duplicated_tokens",
        "clone_groups",
        "clone_instances",
        "duplication_percentage"
      ],
      "description": "Aggregate duplication statistics for the analyzed project.",
      "properties": {
        "total_files": {
          "type": "integer",
          "description": "Total files analyzed."
        },
        "files_with_clones": {
          "type": "integer",
          "description": "Files containing at least one clone instance."
        },
        "total_lines": {
          "type": "integer",
          "description": "Total lines across all analyzed files."
        },
        "duplicated_lines": {
          "type": "integer",
          "description": "Lines that are part of at least one clone."
        },
        "total_tokens": {
          "type": "integer",
          "description": "Total tokens across all analyzed files."
        },
        "duplicated_tokens": {
          "type": "integer",
          "description": "Tokens that are part of at least one clone."
        },
        "clone_groups": {
          "type": "integer",
          "description": "Number of clone groups in the reported `clone_groups[]` array. Matches `clone_groups[].length` post `minOccurrences` filtering; the count of groups hidden by the filter is exposed in `clone_groups_below_min_occurrences`."
        },
        "clone_instances": {
          "type": "integer",
          "description": "Total clone instances across all reported groups. Matches `clone_groups[]`'s flattened instance count post `minOccurrences` filtering."
        },
        "duplication_percentage": {
          "type": "number",
          "description": "Percentage of duplicated lines (0.0 to 100.0). Always reflects the FULL corpus, computed BEFORE the `minOccurrences` filter so trend lines and `threshold` gates stay stable when the filter changes.",
          "minimum": 0,
          "maximum": 100
        },
        "clone_groups_below_min_occurrences": {
          "type": "integer",
          "description": "Number of clone groups hidden by `duplicates.minOccurrences`. Absent (or `0`) when the filter is at its default of `2` and nothing was hidden. Pre-filter clone group count = `clone_groups + clone_groups_below_min_occurrences`."
        }
      }
    },
    "RegressionResult": {
      "type": "object",
      "properties": {
        "status": {
          "description": "Outcome of the regression check.",
          "allOf": [
            {
              "$ref": "#/definitions/RegressionStatus"
            }
          ]
        },
        "baseline_total": {
          "type": [
            "integer",
            "null"
          ],
          "format": "int64",
          "description": "Baseline total before the change. Absent when status is `skipped`."
        },
        "current_total": {
          "type": [
            "integer",
            "null"
          ],
          "format": "int64",
          "description": "Current total after the change. Absent when status is `skipped`."
        },
        "delta": {
          "type": [
            "integer",
            "null"
          ],
          "format": "int64",
          "description": "Difference current - baseline. Absent when status is `skipped`."
        },
        "tolerance": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Configured tolerance, interpreted per [`RegressionToleranceKind`].\nAbsent when status is `skipped`."
        },
        "tolerance_kind": {
          "anyOf": [
            {
              "$ref": "#/definitions/RegressionToleranceKind"
            },
            {
              "type": "null"
            }
          ],
          "description": "Interpretation of the tolerance value."
        },
        "exceeded": {
          "type": "boolean",
          "description": "Whether the regression exceeded the tolerance."
        },
        "reason": {
          "type": [
            "string",
            "null"
          ],
          "description": "Only present when status is `skipped`."
        }
      },
      "required": [
        "status",
        "exceeded"
      ],
      "description": "Result of regression detection (`--fail-on-regression`). Compares current\nissue counts against a baseline from config or an explicit file."
    },
    "EntryPoints": {
      "type": "object",
      "properties": {
        "total": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of detected entry points."
        },
        "sources": {
          "type": "object",
          "additionalProperties": {
            "type": "integer",
            "format": "uint",
            "minimum": 0
          },
          "description": "Breakdown of entry points by detection source (e.g., `\"package.json\"`,\n`\"next.js\"`, `\"config entry\"`). Underscored keys so dashboards can\ndrill into individual sources."
        }
      },
      "required": [
        "total",
        "sources"
      ],
      "description": "Entry-point detection summary embedded in `CheckOutput` and the combined\nenvelope."
    },
    "CheckSummary": {
      "type": "object",
      "properties": {
        "total_issues": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of issues across all categories."
        },
        "unused_files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Unused source files."
        },
        "unused_exports": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Unused value exports."
        },
        "unused_types": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Unused type exports."
        },
        "private_type_leaks": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Public exports whose signature references same-file private types."
        },
        "unused_dependencies": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Combined count of unused entries across `dependencies`,\n`devDependencies`, and `optionalDependencies`. The per-section\nbreakdown lives in the individual issue arrays on `CheckOutput`."
        },
        "unused_enum_members": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Unused enum members."
        },
        "unused_class_members": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Unused class members."
        },
        "unresolved_imports": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Imports that could not be resolved against the project's module graph."
        },
        "unlisted_dependencies": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Dependencies imported but absent from `package.json`."
        },
        "duplicate_exports": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Same-named exports declared in more than one module."
        },
        "type_only_dependencies": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Production dependencies only used via type-only imports (could be\ndevDependencies). Only populated in production mode."
        },
        "test_only_dependencies": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Production dependencies only imported by test files (could be\ndevDependencies)."
        },
        "circular_dependencies": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Cycles detected in the import graph."
        },
        "boundary_violations": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Imports that cross architecture boundary rules."
        },
        "stale_suppressions": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Suppression comments that no longer match a finding."
        },
        "unused_catalog_entries": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Unused pnpm-workspace catalog entries."
        },
        "empty_catalog_groups": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Empty named catalog groups."
        },
        "unresolved_catalog_references": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Workspace package.json catalog references the workspace catalogs\ndo not declare."
        },
        "unused_dependency_overrides": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Pnpm `overrides:` entries whose target package is not declared by any\nworkspace package and not present in the lockfile."
        },
        "misconfigured_dependency_overrides": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Pnpm `overrides:` entries whose key or value cannot be parsed."
        }
      },
      "required": [
        "total_issues",
        "unused_files",
        "unused_exports",
        "unused_types",
        "private_type_leaks",
        "unused_dependencies",
        "unused_enum_members",
        "unused_class_members",
        "unresolved_imports",
        "unlisted_dependencies",
        "duplicate_exports",
        "type_only_dependencies",
        "test_only_dependencies",
        "circular_dependencies",
        "boundary_violations",
        "stale_suppressions",
        "unused_catalog_entries",
        "empty_catalog_groups",
        "unresolved_catalog_references",
        "unused_dependency_overrides",
        "misconfigured_dependency_overrides"
      ],
      "description": "Per-category issue counts for dead-code analysis. Always present in\n`CheckOutput`; when `--summary` is used the individual issue arrays are\nomitted but this object stays populated."
    },
    "BaselineDeltas": {
      "type": "object",
      "properties": {
        "total_delta": {
          "type": "integer",
          "format": "int64",
          "description": "Net change in total issues vs baseline (positive = more issues)."
        },
        "per_category": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/BaselineCategoryDelta"
          },
          "description": "Per-category breakdown of current, baseline, and delta counts."
        }
      },
      "required": [
        "total_delta",
        "per_category"
      ],
      "description": "Per-category delta comparison against a saved baseline. Only present in\n`CheckOutput` when `--baseline` is used."
    },
    "BaselineMatch": {
      "type": "object",
      "properties": {
        "entries": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of entries in the loaded baseline file."
        },
        "matched": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of baseline entries that matched current issues and were\nfiltered."
        }
      },
      "required": [
        "entries",
        "matched"
      ],
      "description": "Baseline match statistics. Shows how many baseline entries existed and how\nmany matched current issues. Useful for detecting stale baselines\nprogrammatically. Only present in `CheckOutput` when `--baseline` is used."
    },
    "CodeClimateOutput": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/CodeClimateIssue"
      },
      "description": "Envelope emitted by `fallow --format codeclimate` and\n`fallow --format gitlab-codequality`. GitLab Code Quality consumes the\nsame shape. The wire form is a bare JSON array, not an object."
    },
    "CodeClimateIssue": {
      "type": "object",
      "properties": {
        "type": {
          "description": "Always the literal string `\"issue\"`.",
          "allOf": [
            {
              "$ref": "#/definitions/CodeClimateIssueKind"
            }
          ]
        },
        "check_name": {
          "type": "string",
          "description": "Fallow rule identifier (always starts with `fallow/`)."
        },
        "description": {
          "type": "string",
          "description": "Human-readable description of the finding."
        },
        "categories": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Free-form categories applied by the report renderer."
        },
        "severity": {
          "description": "CodeClimate-style severity.",
          "allOf": [
            {
              "$ref": "#/definitions/CodeClimateSeverity"
            }
          ]
        },
        "fingerprint": {
          "type": "string",
          "description": "Stable fingerprint used by CI dashboards to deduplicate findings\nacross runs."
        },
        "location": {
          "description": "File path + start line of the finding.",
          "allOf": [
            {
              "$ref": "#/definitions/CodeClimateLocation"
            }
          ]
        }
      },
      "required": [
        "type",
        "check_name",
        "description",
        "categories",
        "severity",
        "fingerprint",
        "location"
      ],
      "description": "Single CodeClimate-compatible issue inside [`CodeClimateOutput`]."
    },
    "ReviewEnvelopeOutput": {
      "type": "object",
      "properties": {
        "event": {
          "anyOf": [
            {
              "$ref": "#/definitions/ReviewEnvelopeEvent"
            },
            {
              "type": "null"
            }
          ],
          "description": "GitHub review event. Omitted for GitLab."
        },
        "body": {
          "type": "string",
          "description": "Review summary body (rendered above per-line comments)."
        },
        "comments": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ReviewComment"
          },
          "description": "Per-line comments. Each is either a [`GitHubReviewComment`] or a\n[`GitLabReviewComment`] depending on `meta.provider`."
        },
        "meta": {
          "description": "Envelope metadata block.",
          "allOf": [
            {
              "$ref": "#/definitions/ReviewEnvelopeMeta"
            }
          ]
        }
      },
      "required": [
        "body",
        "comments",
        "meta"
      ],
      "description": "Envelope emitted by `fallow --format review-github` / `review-gitlab`.\nConsumed by `action/scripts/review.sh` and `ci/scripts/review.sh` to\npost inline PR / MR review comments."
    },
    "GitHubReviewComment": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File path the comment targets, repo-root relative."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-indexed line number the comment targets."
        },
        "side": {
          "description": "Always the literal string `\"RIGHT\"`; GitHub review comments target\ncurrent-state/new-side lines; deletion-side comments are not modeled\nyet.",
          "allOf": [
            {
              "$ref": "#/definitions/GitHubReviewSide"
            }
          ]
        },
        "body": {
          "type": "string",
          "description": "Markdown body of the comment."
        },
        "fingerprint": {
          "type": "string",
          "description": "Stable fingerprint for the comment, used by `fallow ci\nreconcile-review` to detect carryover comments across PR revisions."
        }
      },
      "required": [
        "path",
        "line",
        "side",
        "body",
        "fingerprint"
      ],
      "description": "GitHub pull-request review comment."
    },
    "GitLabReviewComment": {
      "type": "object",
      "properties": {
        "body": {
          "type": "string",
          "description": "Markdown body of the comment."
        },
        "position": {
          "description": "Position block describing where the comment attaches on the diff.",
          "allOf": [
            {
              "$ref": "#/definitions/GitLabReviewPosition"
            }
          ]
        },
        "fingerprint": {
          "type": "string",
          "description": "Stable fingerprint for the comment."
        }
      },
      "required": [
        "body",
        "position",
        "fingerprint"
      ],
      "description": "GitLab merge-request discussion comment."
    },
    "GitLabReviewPosition": {
      "type": "object",
      "properties": {
        "base_sha": {
          "type": [
            "string",
            "null"
          ],
          "description": "Merge-request base SHA."
        },
        "start_sha": {
          "type": [
            "string",
            "null"
          ],
          "description": "Merge-request start SHA."
        },
        "head_sha": {
          "type": [
            "string",
            "null"
          ],
          "description": "Merge-request head SHA."
        },
        "position_type": {
          "description": "Always `\"text\"` today.",
          "allOf": [
            {
              "$ref": "#/definitions/GitLabReviewPositionType"
            }
          ]
        },
        "old_path": {
          "type": "string",
          "description": "File path on the base side."
        },
        "new_path": {
          "type": "string",
          "description": "File path on the head side."
        },
        "new_line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-indexed line on the head side."
        }
      },
      "required": [
        "position_type",
        "old_path",
        "new_path",
        "new_line"
      ],
      "description": "`position` block inside [`GitLabReviewComment`]. Mirrors the GitLab\nmerge-request discussion-position API."
    },
    "ReviewReconcileOutput": {
      "type": "object",
      "properties": {
        "schema": {
          "description": "Envelope schema marker, always `fallow-review-reconcile/v1`.",
          "allOf": [
            {
              "$ref": "#/definitions/ReviewReconcileSchema"
            }
          ]
        },
        "provider": {
          "description": "Which provider this reconcile pass was for.",
          "allOf": [
            {
              "$ref": "#/definitions/ReviewProvider"
            }
          ]
        },
        "target": {
          "type": [
            "string",
            "null"
          ],
          "description": "PR / MR target identifier supplied to `fallow ci reconcile-review`.\n`null` when the command ran without an explicit target."
        },
        "dry_run": {
          "type": "boolean",
          "description": "Whether the reconcile ran in dry-run mode."
        },
        "comments": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Number of comments in the supplied review envelope."
        },
        "current_fingerprints": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Total fingerprints discovered in the supplied envelope."
        },
        "existing_fingerprints": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Existing fingerprints already posted on the PR / MR."
        },
        "new_fingerprints": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Newly-introduced fingerprints (current minus existing)."
        },
        "stale_fingerprints": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Stale fingerprints (existing minus current)."
        },
        "new": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Identifiers of the new fingerprints (subset of comments)."
        },
        "stale": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Identifiers of the stale fingerprints (subset of existing)."
        },
        "provider_warning": {
          "type": [
            "string",
            "null"
          ],
          "description": "Optional warning when the provider API was unreachable or\nauth-rejected. `null` on the happy path."
        },
        "resolution_comments_posted": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Resolution comments actually posted (zero on dry runs)."
        },
        "threads_resolved": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "Stale review threads actually resolved (zero on dry runs)."
        },
        "apply_errors": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Errors collected during apply, one entry per failure."
        }
      },
      "required": [
        "schema",
        "provider",
        "dry_run",
        "comments",
        "current_fingerprints",
        "existing_fingerprints",
        "new_fingerprints",
        "stale_fingerprints",
        "new",
        "stale",
        "resolution_comments_posted",
        "threads_resolved",
        "apply_errors"
      ],
      "description": "Envelope emitted by `fallow ci reconcile-review --format json`. Used by\nCI integrations to drive comment carry-over and stale-comment cleanup\nacross PR / MR revisions."
    },
    "MirroredDirectory": {
      "type": "object",
      "properties": {
        "dir_a": {
          "type": "string",
          "description": "First directory path (lexically smaller)."
        },
        "dir_b": {
          "type": "string",
          "description": "Second directory path."
        },
        "shared_files": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Filenames shared between the two directories."
        },
        "total_lines": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total duplicated lines across all shared files."
        }
      },
      "required": [
        "dir_a",
        "dir_b",
        "shared_files",
        "total_lines"
      ],
      "description": "A detected mirrored directory pattern: two directory prefixes that contain\nidentical files (e.g., `src/` and `deno/lib/`)."
    },
    "VitalSignsCounts": {
      "type": "object",
      "properties": {
        "total_files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "total_exports": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "dead_files": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "dead_exports": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "duplicated_lines": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "total_lines": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "files_scored": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint",
          "minimum": 0
        },
        "total_deps": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "total_files",
        "total_exports",
        "dead_files",
        "dead_exports",
        "total_deps"
      ],
      "description": "Raw counts backing the vital signs percentages.\n\nStored alongside `VitalSigns` in snapshots so that Phase 2b trend reporting\ncan decompose percentage changes into numerator vs denominator shifts."
    },
    "Meta": {
      "type": "object",
      "properties": {
        "docs": {
          "type": [
            "string",
            "null"
          ],
          "description": "URL to the documentation page for this command."
        },
        "metrics": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/MetaMetric"
          },
          "description": "Per-metric definitions: name, description, range, interpretation."
        },
        "rules": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/MetaRule"
          },
          "description": "Per-rule definitions for check command output."
        }
      },
      "description": "Metric and rule definitions emitted under `_meta` when `--explain` is\npassed (always present in MCP responses). Helps AI agents and CI systems\ninterpret metric values without re-reading the docs site."
    },
    "AnalysisResults": {
      "type": "object",
      "properties": {
        "unused_files": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedFile"
          },
          "description": "Files not reachable from any entry point."
        },
        "unused_exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedExport"
          },
          "description": "Exports never imported by other modules."
        },
        "unused_types": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedExport"
          },
          "description": "Type exports never imported by other modules."
        },
        "private_type_leaks": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/PrivateTypeLeak"
          },
          "description": "Exported symbols whose public signature references same-file private types."
        },
        "unused_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Dependencies listed in package.json but never imported."
        },
        "unused_dev_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Dev dependencies listed in package.json but never imported."
        },
        "unused_optional_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependency"
          },
          "description": "Optional dependencies listed in package.json but never imported."
        },
        "unused_enum_members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedMember"
          },
          "description": "Enum members never accessed."
        },
        "unused_class_members": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedMember"
          },
          "description": "Class members never accessed."
        },
        "unresolved_imports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnresolvedImport"
          },
          "description": "Import specifiers that could not be resolved."
        },
        "unlisted_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnlistedDependency"
          },
          "description": "Dependencies used in code but not listed in package.json."
        },
        "duplicate_exports": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/DuplicateExport"
          },
          "description": "Exports with the same name across multiple modules."
        },
        "type_only_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TypeOnlyDependency"
          },
          "description": "Production dependencies only used via type-only imports (could be devDependencies).\nOnly populated in production mode."
        },
        "test_only_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/TestOnlyDependency"
          },
          "description": "Production dependencies only imported by test files (could be devDependencies).",
          "default": []
        },
        "circular_dependencies": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/CircularDependency"
          },
          "description": "Circular dependency chains detected in the module graph."
        },
        "boundary_violations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/BoundaryViolation"
          },
          "description": "Imports that cross architecture boundary rules.",
          "default": []
        },
        "stale_suppressions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/StaleSuppression"
          },
          "description": "Suppression comments or JSDoc tags that no longer match any issue.",
          "default": []
        },
        "unused_catalog_entries": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedCatalogEntry"
          },
          "description": "Entries in pnpm-workspace.yaml catalogs that no workspace package references.",
          "default": []
        },
        "empty_catalog_groups": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EmptyCatalogGroup"
          },
          "description": "Empty named groups under pnpm-workspace.yaml's catalogs: section.",
          "default": []
        },
        "unresolved_catalog_references": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnresolvedCatalogReference"
          },
          "description": "Workspace package.json references to pnpm catalogs that don't declare the package.",
          "default": []
        },
        "unused_dependency_overrides": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/UnusedDependencyOverride"
          },
          "description": "Entries in pnpm `overrides:` / `pnpm.overrides` whose target package is not\ndeclared by any workspace package and not resolved in pnpm-lock.yaml.",
          "default": []
        },
        "misconfigured_dependency_overrides": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MisconfiguredDependencyOverride"
          },
          "description": "Entries in pnpm `overrides:` / `pnpm.overrides` whose key or value cannot be parsed.",
          "default": []
        }
      },
      "required": [
        "unused_files",
        "unused_exports",
        "unused_types",
        "private_type_leaks",
        "unused_dependencies",
        "unused_dev_dependencies",
        "unused_optional_dependencies",
        "unused_enum_members",
        "unused_class_members",
        "unresolved_imports",
        "unlisted_dependencies",
        "duplicate_exports",
        "type_only_dependencies",
        "circular_dependencies"
      ],
      "description": "Complete analysis results.\n\n# Examples\n\n```\nuse fallow_types::results::{AnalysisResults, UnusedFile};\nuse std::path::PathBuf;\n\nlet mut results = AnalysisResults::default();\nassert_eq!(results.total_issues(), 0);\nassert!(!results.has_issues());\n\nresults.unused_files.push(UnusedFile {\n    path: PathBuf::from(\"src/dead.ts\"),\n});\nassert_eq!(results.total_issues(), 1);\nassert!(results.has_issues());\n```"
    },
    "DependencyLocation": {
      "oneOf": [
        {
          "type": "string",
          "const": "dependencies",
          "description": "Listed in `dependencies`."
        },
        {
          "type": "string",
          "const": "devDependencies",
          "description": "Listed in `devDependencies`."
        },
        {
          "type": "string",
          "const": "optionalDependencies",
          "description": "Listed in `optionalDependencies`."
        }
      ],
      "description": "Where in package.json a dependency is listed.\n\n# Examples\n\n```\nuse fallow_types::results::DependencyLocation;\n\n// All three variants are constructible\nlet loc = DependencyLocation::Dependencies;\nlet dev = DependencyLocation::DevDependencies;\nlet opt = DependencyLocation::OptionalDependencies;\n// Debug output includes the variant name\nassert!(format!(\"{loc:?}\").contains(\"Dependencies\"));\nassert!(format!(\"{dev:?}\").contains(\"DevDependencies\"));\nassert!(format!(\"{opt:?}\").contains(\"OptionalDependencies\"));\n```"
    },
    "MemberKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "enum_member",
          "description": "A TypeScript enum member."
        },
        {
          "type": "string",
          "const": "class_method",
          "description": "A class method."
        },
        {
          "type": "string",
          "const": "class_property",
          "description": "A class property."
        },
        {
          "type": "string",
          "const": "namespace_member",
          "description": "A member exported from a TypeScript namespace."
        }
      ],
      "description": "The kind of member.\n\n# Examples\n\n```\nuse fallow_types::extract::MemberKind;\n\nlet kind = MemberKind::EnumMember;\nassert_eq!(kind, MemberKind::EnumMember);\nassert_ne!(kind, MemberKind::ClassMethod);\nassert_ne!(MemberKind::ClassMethod, MemberKind::ClassProperty);\n```"
    },
    "SuppressionOrigin": {
      "oneOf": [
        {
          "type": "object",
          "properties": {
            "issue_kind": {
              "type": [
                "string",
                "null"
              ],
              "description": "The issue kind token from the comment (e.g., \"unused-exports\"), or None for blanket."
            },
            "is_file_level": {
              "type": "boolean",
              "description": "Whether this was a file-level suppression."
            },
            "type": {
              "type": "string",
              "const": "comment"
            }
          },
          "required": [
            "type",
            "is_file_level"
          ],
          "description": "A `// fallow-ignore-next-line` or `// fallow-ignore-file` comment."
        },
        {
          "type": "object",
          "properties": {
            "export_name": {
              "type": "string",
              "description": "The name of the export that was tagged."
            },
            "type": {
              "type": "string",
              "const": "jsdoc_tag"
            }
          },
          "required": [
            "type",
            "export_name"
          ],
          "description": "An `@expected-unused` JSDoc tag on an export."
        }
      ],
      "description": "The origin of a stale suppression: inline comment or JSDoc tag."
    },
    "DependencyOverrideSource": {
      "oneOf": [
        {
          "type": "string",
          "const": "pnpm-workspace.yaml",
          "description": "Top-level `overrides:` key in `pnpm-workspace.yaml`."
        },
        {
          "type": "string",
          "const": "package.json",
          "description": "`pnpm.overrides` in a root `package.json`."
        }
      ],
      "description": "Where an override entry was declared. Serialized as the filename label\n(`\"pnpm-workspace.yaml\"` or `\"package.json\"`) so the value in JSON output\nmatches the value users write in `ignoreDependencyOverrides[].source`."
    },
    "DependencyOverrideMisconfigReason": {
      "oneOf": [
        {
          "type": "string",
          "const": "unparsable-key",
          "description": "The override key could not be parsed into a recognised pnpm shape\n(e.g. dangling `>`, missing target, garbage characters)."
        },
        {
          "type": "string",
          "const": "empty-value",
          "description": "The override value is missing, empty, or contains line breaks."
        }
      ],
      "description": "Why a dependency-override entry is misconfigured. `pnpm install` would\neither fail at install time or silently no-op on these entries; surfacing\nthem statically catches the issue before pnpm does."
    },
    "RefactoringKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "ExtractFunction",
          "description": "Extract a shared function/utility."
        },
        {
          "type": "string",
          "const": "ExtractModule",
          "description": "Extract a shared module."
        }
      ],
      "description": "The kind of refactoring suggested for a clone family."
    },
    "FlagKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "environment_variable",
          "description": "Environment variable check (e.g., `process.env.FEATURE_X`)."
        },
        {
          "type": "string",
          "const": "sdk_call",
          "description": "Feature flag SDK call (e.g., `useFlag('name')`, `variation('name', false)`)."
        },
        {
          "type": "string",
          "const": "config_object",
          "description": "Config object property access (e.g., `config.features.newCheckout`)."
        }
      ],
      "description": "The detection method used to identify a feature flag."
    },
    "FlagConfidence": {
      "oneOf": [
        {
          "type": "string",
          "const": "low",
          "description": "Low confidence: heuristic match (config object patterns)."
        },
        {
          "type": "string",
          "const": "medium",
          "description": "Medium confidence: pattern match with some ambiguity."
        },
        {
          "type": "string",
          "const": "high",
          "description": "High confidence: unambiguous pattern (env vars, direct SDK calls)."
        }
      ],
      "description": "Detection confidence for a feature flag finding."
    },
    "FeatureFlag": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the feature flag usage."
        },
        "flag_name": {
          "type": "string",
          "description": "Name or identifier of the flag (e.g., `ENABLE_NEW_CHECKOUT`, `new-checkout`)."
        },
        "kind": {
          "description": "How the flag was detected.",
          "allOf": [
            {
              "$ref": "#/definitions/FlagKind"
            }
          ]
        },
        "confidence": {
          "description": "Detection confidence level.",
          "allOf": [
            {
              "$ref": "#/definitions/FlagConfidence"
            }
          ]
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        },
        "sdk_name": {
          "type": [
            "string",
            "null"
          ],
          "description": "SDK or provider name (e.g., \"LaunchDarkly\", \"Statsig\"), if detected from SDK call."
        },
        "guarded_dead_exports": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Unused exports found within the guarded code block.\nPopulated by cross-reference with dead code analysis."
        }
      },
      "required": [
        "path",
        "flag_name",
        "kind",
        "confidence",
        "line",
        "col",
        "guarded_dead_exports"
      ],
      "description": "A detected feature flag use site."
    },
    "ExportUsage": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the export."
        },
        "export_name": {
          "type": "string",
          "description": "Name of the exported symbol."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        },
        "reference_count": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Number of files that reference this export."
        },
        "reference_locations": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ReferenceLocation"
          },
          "description": "Locations where this export is referenced. Used by the LSP Code Lens\nto enable click-to-navigate via `editor.action.showReferences`."
        }
      },
      "required": [
        "path",
        "export_name",
        "line",
        "col",
        "reference_count",
        "reference_locations"
      ],
      "description": "Usage count for an export symbol. Used by the LSP Code Lens to show\nreference counts above each export declaration."
    },
    "ReferenceLocation": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File containing the import that references the export."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "col": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "0-based byte column offset."
        }
      },
      "required": [
        "path",
        "line",
        "col"
      ],
      "description": "A location where an export is referenced (import site in another file)."
    },
    "EntryPointSummary": {
      "type": "object",
      "properties": {
        "total": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Total number of entry points detected."
        },
        "by_source": {
          "type": "array",
          "items": {
            "type": "array",
            "maxItems": 2,
            "minItems": 2,
            "items": [
              {
                "type": "string"
              },
              {
                "type": "integer",
                "format": "uint",
                "minimum": 0
              }
            ]
          },
          "description": "Breakdown by source category (e.g., \"package.json\" -> 3, \"plugin\" -> 12).\nSorted by key for deterministic output."
        }
      },
      "required": [
        "total",
        "by_source"
      ],
      "description": "Summary of detected entry points, grouped by discovery source.\n\nUsed to surface entry-point detection status in human and JSON output,\nso library authors can verify that fallow found the right entry points."
    },
    "FixActionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "remove-export",
          "description": "Remove an export declaration from a source file."
        },
        {
          "type": "string",
          "const": "delete-file",
          "description": "Delete an entire unused file."
        },
        {
          "type": "string",
          "const": "remove-dependency",
          "description": "Remove an entry from `dependencies` / `devDependencies` in\n`package.json`."
        },
        {
          "type": "string",
          "const": "move-dependency",
          "description": "Move an entry between `dependencies` and `devDependencies`."
        },
        {
          "type": "string",
          "const": "remove-enum-member",
          "description": "Remove an enum member from a TypeScript enum."
        },
        {
          "type": "string",
          "const": "remove-class-member",
          "description": "Remove a class member (method or property)."
        },
        {
          "type": "string",
          "const": "resolve-import",
          "description": "Resolve an unresolved import (manual)."
        },
        {
          "type": "string",
          "const": "install-dependency",
          "description": "Install a missing dependency."
        },
        {
          "type": "string",
          "const": "remove-duplicate",
          "description": "Remove a duplicate export (the canonical action for\n`duplicate-exports`)."
        },
        {
          "type": "string",
          "const": "move-to-dev",
          "description": "Move a production dependency to `devDependencies`\n(used by type-only-dependency and test-only-dependency findings)."
        },
        {
          "type": "string",
          "const": "refactor-cycle",
          "description": "Break a circular dependency by refactoring imports."
        },
        {
          "type": "string",
          "const": "refactor-boundary",
          "description": "Resolve a boundary violation by refactoring the import."
        },
        {
          "type": "string",
          "const": "export-type",
          "description": "Convert an import statement to a type-only import (used by\nprivate-type-leak findings)."
        },
        {
          "type": "string",
          "const": "remove-catalog-entry",
          "description": "Remove an unused catalog entry from `pnpm-workspace.yaml`."
        },
        {
          "type": "string",
          "const": "remove-empty-catalog-group",
          "description": "Remove an empty named catalog group from `pnpm-workspace.yaml`."
        },
        {
          "type": "string",
          "const": "update-catalog-reference",
          "description": "Update an existing `catalog:` reference in a workspace `package.json`\nto point at a different (declared) catalog."
        },
        {
          "type": "string",
          "const": "add-catalog-entry",
          "description": "Add the missing entry to the referenced catalog."
        },
        {
          "type": "string",
          "const": "remove-catalog-reference",
          "description": "Remove the catalog reference from the workspace `package.json` and\nreplace it with a hardcoded version."
        },
        {
          "type": "string",
          "const": "remove-dependency-override",
          "description": "Remove an unused dependency override entry."
        },
        {
          "type": "string",
          "const": "fix-dependency-override",
          "description": "Fix a misconfigured dependency override entry (unparsable key or empty\nvalue)."
        }
      ],
      "description": "Discriminant string for [`FixAction`]. Kebab-case per the JSON output\ncontract."
    },
    "SuppressLineKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "suppress-line",
          "description": "`// fallow-ignore-next-line <kind>` directive."
        }
      ],
      "description": "Singleton discriminant for [`SuppressLineAction`]."
    },
    "SuppressAutoFixable": {
      "oneOf": [
        {
          "type": "string",
          "const": "false",
          "description": "Always false for suppress actions."
        }
      ],
      "description": "`auto_fixable` field on suppress actions: always literally `false` per the\npublic contract, but typed as a singleton enum so the derived schema\nrenders it as `\"const\": false` rather than the generic `boolean`."
    },
    "SuppressLineScope": {
      "oneOf": [
        {
          "type": "string",
          "const": "per-location",
          "description": "Apply the suppression comment at each location of the multi-location\nfinding (e.g., every `duplicate_exports` site)."
        }
      ],
      "description": "Scope marker for line suppressions that span multiple locations."
    },
    "SuppressFileKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "suppress-file",
          "description": "`// fallow-ignore-file <kind>` directive."
        }
      ],
      "description": "Singleton discriminant for [`SuppressFileAction`]."
    },
    "AddToConfigKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "add-to-config",
          "description": "Append a value into a fallow config `ignore*` list."
        }
      ],
      "description": "Singleton discriminant for [`AddToConfigAction`]."
    },
    "AddToConfigValue": {
      "anyOf": [
        {
          "type": "string",
          "description": "Scalar string value (e.g., a package name for\n`ignoreDependencies: [\"lodash\"]`)."
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/IgnoreExportsRule"
          },
          "description": "Array of file+export rule objects for `ignoreExports`."
        },
        {
          "type": "object",
          "additionalProperties": true,
          "description": "Free-form object for rule-shaped keys like\n`ignoreCatalogReferences` / `ignoreDependencyOverrides`. The shape\nmatches the rule entry users add to their fallow config; consumers\nvalidate against the per-key schema referenced by `value_schema`."
        }
      ],
      "description": "Value payload for [`AddToConfigAction::value`]. The variants line up with\nthe documented per-`config_key` shapes; deserialization is untagged so\ndownstream consumers can switch on the JSON value's type."
    },
    "IgnoreExportsRule": {
      "type": "object",
      "properties": {
        "file": {
          "type": "string",
          "description": "File path (forward slashes, relative to project root) to which this\nrule applies. Globs are accepted."
        },
        "exports": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names of exports inside `file` to silently treat as used."
        }
      },
      "required": [
        "file",
        "exports"
      ],
      "description": "Single `ignoreExports` rule entry. The fallow config accepts an array of\nthese under the `ignoreExports` key."
    },
    "CoverageModel": {
      "oneOf": [
        {
          "type": "string",
          "const": "static_binary",
          "description": "Binary model: test-reachable = CC, untested = CC^2 + CC.\nSuperseded by `StaticEstimated`; retained for serialization compatibility."
        },
        {
          "type": "string",
          "const": "static_estimated",
          "description": "Graph-based estimation: per-function coverage derived from export\nreference analysis. Directly test-referenced = 85%, indirectly\ntest-reachable = 40%, untested = 0%. Default model."
        },
        {
          "type": "string",
          "const": "istanbul",
          "description": "Istanbul-format coverage data: real per-function statement coverage\nfrom Jest, Vitest, c8, nyc, or any Istanbul-compatible tool.\nCRAP = CC^2 * (1 - cov/100)^3 + CC."
        }
      ],
      "description": "Coverage model used for CRAP score computation."
    },
    "ExceededThreshold": {
      "oneOf": [
        {
          "type": "string",
          "const": "cyclomatic",
          "description": "Only cyclomatic exceeded."
        },
        {
          "type": "string",
          "const": "cognitive",
          "description": "Only cognitive exceeded."
        },
        {
          "type": "string",
          "const": "both",
          "description": "Both cyclomatic and cognitive exceeded (may or may not also exceed CRAP)."
        },
        {
          "type": "string",
          "const": "crap",
          "description": "Only CRAP exceeded (cyclomatic and cognitive are under threshold)."
        },
        {
          "type": "string",
          "const": "cyclomatic_crap",
          "description": "Cyclomatic and CRAP exceeded."
        },
        {
          "type": "string",
          "const": "cognitive_crap",
          "description": "Cognitive and CRAP exceeded."
        },
        {
          "type": "string",
          "const": "all",
          "description": "Cyclomatic, cognitive, and CRAP all exceeded."
        }
      ],
      "description": "Which complexity threshold was exceeded."
    },
    "FindingSeverity": {
      "oneOf": [
        {
          "type": "string",
          "const": "moderate",
          "description": "Above threshold but manageable (cognitive < 25 or cyclomatic < 30)."
        },
        {
          "type": "string",
          "const": "high",
          "description": "Recommended for extraction (cognitive 25-39 or cyclomatic 30-49)."
        },
        {
          "type": "string",
          "const": "critical",
          "description": "Immediate extraction candidate (cognitive >= 40 or cyclomatic >= 50)."
        }
      ],
      "description": "Severity tier indicating how far a function exceeds complexity thresholds.\n\nDetermined by the highest tier reached across both cognitive and cyclomatic\nscores. Default thresholds: cognitive 25/40, cyclomatic 30/50."
    },
    "CoverageTier": {
      "oneOf": [
        {
          "type": "string",
          "const": "none",
          "description": "0% coverage: file is not test-reachable, or Istanbul reports 0%."
        },
        {
          "type": "string",
          "const": "partial",
          "description": "Some coverage: estimated 40% band, or Istanbul reports `(0, 70)`."
        },
        {
          "type": "string",
          "const": "high",
          "description": "High coverage: estimated 85% band, or Istanbul reports `>= 70`."
        }
      ],
      "description": "Coverage tier classification for CRAP findings.\n\nBucketed coverage signal that lets action consumers (AI agents, IDE\nextensions, CI integrations) pick the right remediation without knowing\nthe underlying coverage values:\n- `None`: file has no test reachability (estimated model 0% band) or\n  Istanbul data shows 0% statement coverage. The right action is\n  \"add tests from scratch.\"\n- `Partial`: some coverage exists (estimated model 40% band, or\n  Istanbul shows >0% but below the high watermark). The right\n  action is \"increase coverage on uncovered branches.\"\n- `High`: coverage is at or above the high watermark (estimated model\n  85% band, or Istanbul shows >= 70%). Action selection still checks\n  the CRAP formula before deciding whether coverage or refactoring is\n  the better remediation.\n\nThe high watermark default is 70 (matches Istanbul `lines: 70`).\nPartial is anything in `(0, 70)`. None is `<= 0`."
    },
    "ChurnTrend": {
      "oneOf": [
        {
          "type": "string",
          "const": "accelerating",
          "description": "Recent half has >1.5× the commits of the older half."
        },
        {
          "type": "string",
          "const": "stable",
          "description": "Churn is roughly stable between halves."
        },
        {
          "type": "string",
          "const": "cooling",
          "description": "Recent half has <0.67× the commits of the older half."
        }
      ],
      "description": "Churn trend indicator based on comparing recent vs older halves of the analysis period."
    },
    "ContributorIdentifierFormat": {
      "oneOf": [
        {
          "type": "string",
          "const": "raw",
          "description": "Raw author email as recorded in git history."
        },
        {
          "type": "string",
          "const": "handle",
          "description": "Local-part of the author email, with GitHub-style numeric noreply\nprefixes unwrapped (`12345+alice@users.noreply.github.com` → `alice`)."
        },
        {
          "type": "string",
          "const": "hash",
          "description": "Non-cryptographic stable pseudonym (`xxh3:<16hex>`)."
        }
      ],
      "description": "Format discriminator for [`ContributorEntry::identifier`]."
    },
    "RecommendationCategory": {
      "oneOf": [
        {
          "type": "string",
          "const": "urgent_churn_complexity",
          "description": "Actively-changing file with growing complexity — highest urgency."
        },
        {
          "type": "string",
          "const": "break_circular_dependency",
          "description": "File participates in an import cycle with significant blast radius."
        },
        {
          "type": "string",
          "const": "split_high_impact",
          "description": "High fan-in + high complexity — changes here ripple widely."
        },
        {
          "type": "string",
          "const": "remove_dead_code",
          "description": "Majority of exports are unused — reduce surface area."
        },
        {
          "type": "string",
          "const": "extract_complex_functions",
          "description": "Contains functions with very high cognitive complexity."
        },
        {
          "type": "string",
          "const": "extract_dependencies",
          "description": "Excessive imports reduce testability and increase coupling."
        },
        {
          "type": "string",
          "const": "add_test_coverage",
          "description": "Multiple complex functions lack test dependency path."
        }
      ],
      "description": "Category of refactoring recommendation."
    },
    "EffortEstimate": {
      "oneOf": [
        {
          "type": "string",
          "const": "low",
          "description": "Small file, few functions, low fan-in — quick to address."
        },
        {
          "type": "string",
          "const": "medium",
          "description": "Moderate size or coupling — needs planning."
        },
        {
          "type": "string",
          "const": "high",
          "description": "Large file, many functions, or high fan-in — significant effort."
        }
      ],
      "description": "A ranked refactoring recommendation for a file.\n\n## Priority Formula\n\n```text\npriority = min(density, 1) × 30 + hotspot_boost × 25 + dead_code × 20 + fan_in_norm × 15 + fan_out_norm × 10\n```\n\nFan-in and fan-out normalization uses adaptive percentile-based thresholds\n(p95 of the project distribution, with floors) instead of fixed constants.\n\n## Efficiency (default sort)\n\n```text\nefficiency = priority / effort_numeric   (Low=1, Medium=2, High=3)\n```\n\nSurfaces quick wins: high-priority, low-effort targets rank first.\nEffort estimate for a refactoring target."
    },
    "Confidence": {
      "oneOf": [
        {
          "type": "string",
          "const": "high",
          "description": "Recommendation based on deterministic analysis (graph, AST)."
        },
        {
          "type": "string",
          "const": "medium",
          "description": "Recommendation based on heuristic thresholds."
        },
        {
          "type": "string",
          "const": "low",
          "description": "Recommendation depends on external data quality (git history)."
        }
      ],
      "description": "Confidence level for a refactoring recommendation.\n\nBased on the data source reliability:\n- **High**: deterministic graph/AST analysis (dead code, circular deps, complexity)\n- **Medium**: heuristic thresholds (fan-in/fan-out coupling)\n- **Low**: depends on git history quality (churn-based recommendations)"
    },
    "ContributingFactor": {
      "type": "object",
      "properties": {
        "metric": {
          "type": "string",
          "description": "Metric name (matches JSON field names: `\"fan_in\"`, `\"dead_code_ratio\"`, etc.)."
        },
        "value": {
          "type": "number",
          "format": "double",
          "description": "Raw metric value for programmatic use."
        },
        "threshold": {
          "type": "number",
          "format": "double",
          "description": "Threshold that was exceeded."
        },
        "detail": {
          "type": "string",
          "description": "Human-readable explanation."
        }
      },
      "required": [
        "metric",
        "value",
        "threshold",
        "detail"
      ],
      "description": "A contributing factor that triggered or strengthened a recommendation."
    },
    "TargetEvidence": {
      "type": "object",
      "properties": {
        "unused_exports": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Names of unused exports (populated for `RemoveDeadCode` targets)."
        },
        "complex_functions": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/EvidenceFunction"
          },
          "description": "Complex functions with line numbers and cognitive scores (populated for `ExtractComplexFunctions`)."
        },
        "cycle_path": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "description": "Files forming the import cycle (populated for `BreakCircularDependency` targets)."
        }
      },
      "required": [
        "unused_exports",
        "complex_functions",
        "cycle_path"
      ],
      "description": "Evidence linking a target back to specific analysis data.\n\nProvides enough detail for an AI agent to act on a recommendation\nwithout a second tool call."
    },
    "EvidenceFunction": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Function name."
        },
        "line": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based line number."
        },
        "cognitive": {
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "description": "Cognitive complexity score."
        }
      },
      "required": [
        "name",
        "line",
        "cognitive"
      ],
      "description": "A function referenced in target evidence."
    },
    "TrendPoint": {
      "type": "object",
      "properties": {
        "timestamp": {
          "type": "string",
          "description": "ISO 8601 timestamp of the snapshot."
        },
        "git_sha": {
          "type": [
            "string",
            "null"
          ],
          "description": "Git SHA at time of snapshot."
        },
        "score": {
          "type": [
            "number",
            "null"
          ],
          "format": "double",
          "description": "Health score from the snapshot (stored, not re-derived)."
        },
        "grade": {
          "type": [
            "string",
            "null"
          ],
          "description": "Letter grade from the snapshot."
        },
        "coverage_model": {
          "anyOf": [
            {
              "$ref": "#/definitions/CoverageModel"
            },
            {
              "type": "null"
            }
          ],
          "description": "Coverage model used for CRAP computation in this snapshot."
        },
        "snapshot_schema_version": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint32",
          "minimum": 0,
          "description": "Schema version of the compared snapshot."
        }
      },
      "required": [
        "timestamp"
      ],
      "description": "A reference to a snapshot used in trend comparison."
    },
    "TrendMetric": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Metric identifier (e.g., `\"score\"`, `\"dead_file_pct\"`)."
        },
        "label": {
          "type": "string",
          "description": "Human-readable label (e.g., `\"Health Score\"`, `\"Dead Files\"`)."
        },
        "previous": {
          "type": "number",
          "format": "double",
          "description": "Previous value (from snapshot)."
        },
        "current": {
          "type": "number",
          "format": "double",
          "description": "Current value (from this run)."
        },
        "delta": {
          "type": "number",
          "format": "double",
          "description": "Absolute change (current − previous)."
        },
        "direction": {
          "description": "Direction of change.",
          "allOf": [
            {
              "$ref": "#/definitions/TrendDirection"
            }
          ]
        },
        "unit": {
          "type": "string",
          "description": "Unit for display (e.g., `\"%\"`, `\"\"`, `\"pts\"`)."
        },
        "previous_count": {
          "anyOf": [
            {
              "$ref": "#/definitions/TrendCount"
            },
            {
              "type": "null"
            }
          ],
          "description": "Raw count from previous snapshot (for JSON consumers)."
        },
        "current_count": {
          "anyOf": [
            {
              "$ref": "#/definitions/TrendCount"
            },
            {
              "type": "null"
            }
          ],
          "description": "Raw count from current run (for JSON consumers)."
        }
      },
      "required": [
        "name",
        "label",
        "previous",
        "current",
        "delta",
        "direction",
        "unit"
      ],
      "description": "A single metric's trend between two snapshots."
    },
    "TrendDirection": {
      "oneOf": [
        {
          "type": "string",
          "const": "improving",
          "description": "The metric moved in a beneficial direction."
        },
        {
          "type": "string",
          "const": "declining",
          "description": "The metric moved in a detrimental direction."
        },
        {
          "type": "string",
          "const": "stable",
          "description": "The metric stayed within tolerance."
        }
      ],
      "description": "Direction of a metric's change, semantically (improving/declining/stable)."
    },
    "RuntimeCoverageDataSource": {
      "type": "string",
      "enum": [
        "local",
        "cloud"
      ],
      "description": "Runtime coverage source used to produce the summary."
    },
    "BaselineCategoryDelta": {
      "type": "object",
      "properties": {
        "current": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Current issue count for this category."
        },
        "baseline": {
          "type": "integer",
          "format": "uint",
          "minimum": 0,
          "description": "Baseline issue count for this category."
        },
        "delta": {
          "type": "integer",
          "format": "int64",
          "description": "Change from baseline (current - baseline)."
        }
      },
      "required": [
        "current",
        "baseline",
        "delta"
      ],
      "description": "Single-category baseline delta entry inside [`BaselineDeltas::per_category`]."
    },
    "RegressionStatus": {
      "oneOf": [
        {
          "type": "string",
          "const": "pass",
          "description": "Issue count within tolerance."
        },
        {
          "type": "string",
          "const": "exceeded",
          "description": "Issue count exceeded tolerance."
        },
        {
          "type": "string",
          "const": "skipped",
          "description": "Regression check did not run (missing baseline, etc.)."
        }
      ],
      "description": "Status of a regression-check pass."
    },
    "RegressionToleranceKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "absolute",
          "description": "Tolerance is interpreted as an absolute issue-count delta."
        },
        {
          "type": "string",
          "const": "percentage",
          "description": "Tolerance is interpreted as a percentage of the baseline total."
        }
      ],
      "description": "Interpretation of [`RegressionResult::tolerance`]."
    },
    "MetaMetric": {
      "type": "object",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "Human-readable metric name."
        },
        "description": {
          "type": [
            "string",
            "null"
          ],
          "description": "What this metric measures and how it is computed."
        },
        "range": {
          "type": [
            "string",
            "null"
          ],
          "description": "Valid value range (e.g., `\"[0, 100]\"`)."
        },
        "interpretation": {
          "type": [
            "string",
            "null"
          ],
          "description": "How to read the value (e.g., `\"lower is better\"`)."
        }
      },
      "description": "Single-metric definition inside [`Meta::metrics`]."
    },
    "MetaRule": {
      "type": "object",
      "properties": {
        "name": {
          "type": [
            "string",
            "null"
          ],
          "description": "Human-readable rule name."
        },
        "description": {
          "type": [
            "string",
            "null"
          ],
          "description": "What this rule detects."
        },
        "docs": {
          "type": [
            "string",
            "null"
          ],
          "description": "URL to the rule documentation."
        }
      },
      "description": "Single-rule definition inside [`Meta::rules`]."
    },
    "AuditCommand": {
      "oneOf": [
        {
          "type": "string",
          "const": "audit",
          "description": "The only valid command discriminator for `AuditOutput`."
        }
      ],
      "description": "Singleton `command` discriminator for [`AuditOutput`]."
    },
    "AuditVerdict": {
      "oneOf": [
        {
          "type": "string",
          "const": "pass",
          "description": "No issues in changed files."
        },
        {
          "type": "string",
          "const": "warn",
          "description": "Issues found, but all are warn-severity."
        },
        {
          "type": "string",
          "const": "fail",
          "description": "Error-severity issues found in changed files."
        }
      ],
      "description": "Verdict for the audit command."
    },
    "AuditSummary": {
      "type": "object",
      "properties": {
        "dead_code_issues": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "dead_code_has_errors": {
          "type": "boolean"
        },
        "complexity_findings": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "max_cyclomatic": {
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535
        },
        "duplication_clone_groups": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "dead_code_issues",
        "dead_code_has_errors",
        "complexity_findings",
        "duplication_clone_groups"
      ],
      "description": "Per-category summary counts for the audit result."
    },
    "AuditAttribution": {
      "type": "object",
      "properties": {
        "gate": {
          "$ref": "#/definitions/AuditGate"
        },
        "dead_code_introduced": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "dead_code_inherited": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "complexity_introduced": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "complexity_inherited": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "duplication_introduced": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        },
        "duplication_inherited": {
          "type": "integer",
          "format": "uint",
          "minimum": 0
        }
      },
      "required": [
        "gate",
        "dead_code_introduced",
        "dead_code_inherited",
        "complexity_introduced",
        "complexity_inherited",
        "duplication_introduced",
        "duplication_inherited"
      ],
      "description": "New-vs-inherited issue counts for audit."
    },
    "AuditGate": {
      "oneOf": [
        {
          "type": "string",
          "const": "new-only",
          "description": "Only findings introduced by the current changeset affect the verdict."
        },
        {
          "type": "string",
          "const": "all",
          "description": "All findings in changed files affect the verdict."
        }
      ],
      "description": "Gating mode for `fallow audit`."
    },
    "CoverageSetupSchemaVersion": {
      "oneOf": [
        {
          "type": "string",
          "const": "1",
          "description": "First release of the coverage setup envelope."
        }
      ],
      "description": "Singleton schema-version discriminator for [`CoverageSetupOutput`]."
    },
    "CoverageSetupFramework": {
      "oneOf": [
        {
          "type": "string",
          "const": "nextjs",
          "description": "Next.js (`framework: \"nextjs\"`)."
        },
        {
          "type": "string",
          "const": "nest_js",
          "description": "NestJS (`framework: \"nestjs\"`)."
        },
        {
          "type": "string",
          "const": "nuxt",
          "description": "Nuxt (`framework: \"nuxt\"`)."
        },
        {
          "type": "string",
          "const": "svelte_kit",
          "description": "SvelteKit (`framework: \"sveltekit\"`)."
        },
        {
          "type": "string",
          "const": "astro",
          "description": "Astro (`framework: \"astro\"`)."
        },
        {
          "type": "string",
          "const": "remix",
          "description": "Remix (`framework: \"remix\"`)."
        },
        {
          "type": "string",
          "const": "vite",
          "description": "Vite (`framework: \"vite\"`)."
        },
        {
          "type": "string",
          "const": "plain_node",
          "description": "Plain Node.js (no framework)."
        },
        {
          "type": "string",
          "const": "unknown",
          "description": "Could not determine."
        }
      ],
      "description": "Framework label inside coverage setup output."
    },
    "CoverageSetupPackageManager": {
      "oneOf": [
        {
          "type": "string",
          "const": "npm",
          "description": "`npm`."
        },
        {
          "type": "string",
          "const": "pnpm",
          "description": "`pnpm`."
        },
        {
          "type": "string",
          "const": "yarn",
          "description": "`yarn`."
        },
        {
          "type": "string",
          "const": "bun",
          "description": "`bun`."
        }
      ],
      "description": "Package manager label inside coverage setup output."
    },
    "CoverageSetupRuntimeTarget": {
      "oneOf": [
        {
          "type": "string",
          "const": "node",
          "description": "Node.js runtime target."
        },
        {
          "type": "string",
          "const": "browser",
          "description": "Browser runtime target."
        }
      ],
      "description": "Runtime target inside coverage setup output."
    },
    "GroupByMode": {
      "oneOf": [
        {
          "type": "string",
          "const": "owner",
          "description": "Group by CODEOWNERS team."
        },
        {
          "type": "string",
          "const": "directory",
          "description": "Group by top-level directory prefix."
        },
        {
          "type": "string",
          "const": "package",
          "description": "Group by workspace package name."
        },
        {
          "type": "string",
          "const": "section",
          "description": "Group by GitLab CODEOWNERS `[Section]` header name."
        }
      ],
      "description": "Resolver mode label for grouped envelopes (dead-code, dupes, health).\n\n`owner` groups by CODEOWNERS team, `directory` groups by top-level\ndirectory prefix, `package` groups by workspace package name, `section`\ngroups by GitLab CODEOWNERS `[Section]` header name."
    },
    "CodeClimateIssueKind": {
      "oneOf": [
        {
          "type": "string",
          "const": "issue",
          "description": "The only valid CodeClimate type today."
        }
      ],
      "description": "Discriminator value for [`CodeClimateIssue::kind`]."
    },
    "CodeClimateSeverity": {
      "oneOf": [
        {
          "type": "string",
          "const": "info",
          "description": "Informational."
        },
        {
          "type": "string",
          "const": "minor",
          "description": "Minor finding."
        },
        {
          "type": "string",
          "const": "major",
          "description": "Major finding."
        },
        {
          "type": "string",
          "const": "critical",
          "description": "Critical finding."
        },
        {
          "type": "string",
          "const": "blocker",
          "description": "Blocker (highest severity)."
        }
      ],
      "description": "CodeClimate severity scale."
    },
    "CodeClimateLocation": {
      "type": "object",
      "properties": {
        "path": {
          "type": "string",
          "description": "File path relative to the analysed root."
        },
        "lines": {
          "description": "Wrapper carrying the begin line so the schema lines up with\nCodeClimate's spec.",
          "allOf": [
            {
              "$ref": "#/definitions/CodeClimateLines"
            }
          ]
        }
      },
      "required": [
        "path",
        "lines"
      ],
      "description": "Location block inside [`CodeClimateIssue::location`]."
    },
    "CodeClimateLines": {
      "type": "object",
      "properties": {
        "begin": {
          "type": "integer",
          "format": "uint32",
          "minimum": 0,
          "description": "1-based start line."
        }
      },
      "required": [
        "begin"
      ],
      "description": "`lines.begin` for [`CodeClimateLocation`]."
    },
    "ReviewEnvelopeEvent": {
      "oneOf": [
        {
          "type": "string",
          "const": "COMMENT",
          "description": "GitHub review event for an unblocking comment review."
        }
      ],
      "description": "Singleton GitHub review-event marker."
    },
    "ReviewComment": {
      "anyOf": [
        {
          "description": "GitHub-shaped pull-request review comment.",
          "allOf": [
            {
              "$ref": "#/definitions/GitHubReviewComment"
            }
          ]
        },
        {
          "description": "GitLab-shaped merge-request discussion comment.",
          "allOf": [
            {
              "$ref": "#/definitions/GitLabReviewComment"
            }
          ]
        }
      ],
      "description": "Per-line review comment. Schema is an `anyOf` between GitHub and GitLab\nshapes; at runtime every entry in a single envelope comes from the same\nprovider because the envelope is built from one provider's branch in\n`crates/cli/src/report/ci/review.rs::render_review_envelope`."
    },
    "GitHubReviewSide": {
      "oneOf": [
        {
          "type": "string",
          "const": "RIGHT",
          "description": "GitHub review comments target the new-side line range."
        }
      ],
      "description": "Singleton side discriminator for [`GitHubReviewComment::side`]."
    },
    "GitLabReviewPositionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "text",
          "description": "Plain-text diff position (only kind fallow emits today)."
        }
      ],
      "description": "Singleton position-type discriminator for [`GitLabReviewPosition`]."
    },
    "ReviewEnvelopeMeta": {
      "type": "object",
      "properties": {
        "schema": {
          "description": "Envelope schema marker, always `fallow-review-envelope/v1`.",
          "allOf": [
            {
              "$ref": "#/definitions/ReviewEnvelopeSchema"
            }
          ]
        },
        "provider": {
          "description": "Which provider this envelope is shaped for.",
          "allOf": [
            {
              "$ref": "#/definitions/ReviewProvider"
            }
          ]
        },
        "check_conclusion": {
          "anyOf": [
            {
              "$ref": "#/definitions/ReviewCheckConclusion"
            },
            {
              "type": "null"
            }
          ],
          "description": "Check conclusion derived from the underlying findings. Emitted only\nfor GitHub envelopes today."
        }
      },
      "required": [
        "schema",
        "provider"
      ],
      "description": "`meta` block inside [`ReviewEnvelopeOutput`]."
    },
    "ReviewEnvelopeSchema": {
      "oneOf": [
        {
          "type": "string",
          "const": "fallow-review-envelope/v1",
          "description": "First release of the review envelope format."
        }
      ],
      "description": "Schema-version discriminator for the review envelope."
    },
    "ReviewProvider": {
      "oneOf": [
        {
          "type": "string",
          "const": "github",
          "description": "GitHub pull-request review envelope."
        },
        {
          "type": "string",
          "const": "gitlab",
          "description": "GitLab merge-request discussion envelope."
        }
      ],
      "description": "Review-envelope provider tag."
    },
    "ReviewCheckConclusion": {
      "oneOf": [
        {
          "type": "string",
          "const": "success",
          "description": "No findings."
        },
        {
          "type": "string",
          "const": "neutral",
          "description": "Findings but none gated as failure."
        },
        {
          "type": "string",
          "const": "failure",
          "description": "At least one finding gated as failure."
        }
      ],
      "description": "`meta.check_conclusion` for the GitHub review envelope. Maps to the\nGitHub Checks API conclusion field."
    },
    "ReviewReconcileSchema": {
      "oneOf": [
        {
          "type": "string",
          "const": "fallow-review-reconcile/v1",
          "description": "First release of the review reconcile format."
        }
      ],
      "description": "Schema-version discriminator for the review reconcile envelope."
    },
    "HealthFindingActionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "refactor-function",
          "description": "Refactor the function to reduce complexity. Emitted when\ncyclomatic/cognitive triggered the finding, when full coverage\nstill cannot bring CRAP below `max_crap_threshold`, or as a\nsecondary action when cyclomatic is within 5 of the cyclomatic\nthreshold AND cognitive is at or above the cognitive floor."
        },
        {
          "type": "string",
          "const": "add-tests",
          "description": "Add tests for a CRAP-triggered finding whose coverage tier is\n`none` (no test path reaches the function)."
        },
        {
          "type": "string",
          "const": "increase-coverage",
          "description": "Increase test coverage for a CRAP-triggered finding whose coverage\ntier is `partial` or `high` (some test path exists; add targeted\nassertions for uncovered branches)."
        },
        {
          "type": "string",
          "const": "suppress-file",
          "description": "Suppress with an HTML comment at the top of the template. Used for\nsynthetic Angular `<template>` findings on `.html` files where a\nline suppression cannot be expressed."
        },
        {
          "type": "string",
          "const": "suppress-line",
          "description": "Suppress with an inline `// fallow-ignore-next-line complexity`\ncomment above the function or Angular decorator."
        }
      ],
      "description": "Discriminant for [`HealthFindingAction::kind`]. Mirrors the action types\nemitted by `build_health_finding_actions`. A single finding's `actions`\narray may carry multiple entries of different types: a finding that\nexceeded both cyclomatic and CRAP at `coverage_tier: partial` will get\nBOTH `increase-coverage` AND `refactor-function`, plus the trailing\n`suppress-line`."
    },
    "HotspotActionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "refactor-file",
          "description": "Refactor the hotspot file (high complexity plus frequent change)."
        },
        {
          "type": "string",
          "const": "add-tests",
          "description": "Add test coverage to reduce change risk on the hotspot file."
        },
        {
          "type": "string",
          "const": "low-bus-factor",
          "description": "Bus factor of 1: a single recent contributor owns the file.\nEmitted only with `--ownership`."
        },
        {
          "type": "string",
          "const": "unowned-hotspot",
          "description": "Hotspot matches no CODEOWNERS rule (a rules file exists but no\npattern matches). Emitted only with `--ownership`."
        },
        {
          "type": "string",
          "const": "ownership-drift",
          "description": "Ownership has drifted from the original author to a new top\ncontributor. Emitted only with `--ownership`."
        }
      ],
      "description": "Discriminant for [`HotspotAction::kind`]."
    },
    "HotspotActionHeuristic": {
      "oneOf": [
        {
          "type": "string",
          "const": "directory-deepest",
          "description": "Suggest the deepest directory containing the file (e.g.,\n`/src/api/users/`). Keeps the suggestion reviewable while staying\na directory pattern rather than a per-file rule."
        }
      ],
      "description": "Strategy discriminant for the suggested CODEOWNERS pattern attached to\nan `unowned-hotspot` action."
    },
    "RefactoringTargetActionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "apply-refactoring",
          "description": "Apply the recommended refactoring (extract, split, decouple, etc.)."
        },
        {
          "type": "string",
          "const": "suppress-line",
          "description": "Suppress the underlying complexity finding with an inline comment."
        }
      ],
      "description": "Discriminant for [`RefactoringTargetAction::kind`]."
    },
    "UntestedFileActionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "add-tests",
          "description": "Scaffold tests that exercise the runtime file."
        },
        {
          "type": "string",
          "const": "suppress-file",
          "description": "Suppress coverage-gap reporting for this file with a file-level\ncomment."
        }
      ],
      "description": "Discriminant for [`UntestedFileAction::kind`]. Mirrors the action types\nemitted by `build_untested_file_actions`."
    },
    "UntestedExportActionType": {
      "oneOf": [
        {
          "type": "string",
          "const": "add-test-import",
          "description": "Import and exercise the export from a test-reachable module."
        },
        {
          "type": "string",
          "const": "suppress-file",
          "description": "Suppress coverage-gap reporting for the export's file with a\nfile-level comment."
        }
      ],
      "description": "Discriminant for [`UntestedExportAction::kind`]. Mirrors the action\ntypes emitted by `build_untested_export_actions`."
    }
  }
}
